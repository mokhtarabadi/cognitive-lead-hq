[project]
name = "mcp-persona-server"
version = "1.0.0"
description = "Persona dispatch MCP server (Task 167): QA/reviewer/manager turns over LiteLLM"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.0,<2.0",
    "litellm",
    "mcp-common",
]

[tool.uv.sources]
mcp-common = { path = "../mcp-common", editable = true }
