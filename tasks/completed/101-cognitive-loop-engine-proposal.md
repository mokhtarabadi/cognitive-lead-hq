# Task 101: Cognitive Loop Engine Proposal

**File:** `tasks/qa/101-cognitive-loop-engine-proposal.md`
**Source:** orchestrator
**Type:** feature
**Status:** in-progress

## Source Context

### Variant A: Orchestrator (`**Source:** orchestrator`)

## Goal

Document and preserve the Cognitive Loop Engine architectural proposal for future implementation. This proposal was generated during a multi-agent brainstorming session (Phase 1.5) on 2026-08-18 and explicitly deferred by the Manager due to bandwidth constraints. When bandwidth becomes available, pull this task from backlog and begin Phase 1 implementation.

## Proposal: Cognitive Loop Engine — Automated Brain↔Hands Orchestration

### Executive Summary

The Cognitive Loop Engine is a local orchestration daemon that eliminates the manual copy-paste workflow between the Orchestrator (Brain) and OpenCode/Freebuff (Hands). It routes `system-prompt.md` directly to LLM APIs, invokes the Hands programmatically, and maintains Manager approval gates via Telegram or a local dashboard. The Manager transitions from "data entry operator copying XML blocks" to "executive approving decisions via buttons."

### Core Architecture

```
┌─────────────────────────────────────────────────────────┐
│                  Cognitive Loop Engine                  │
│                                                         │
│  ┌──────────┐   ┌──────────┐   ┌───────────────────┐  │
│  │  Kanban   │──▶│  LLM     │──▶│  Approval         │  │
│  │  Watcher  │   │  Router  │   │  Gateway          │  │
│  │           │   │          │   │  (Telegram/Dash)  │  │
│  └──────────┘   └──────────┘   └───────────────────┘  │
│       ▲                                    │            │
│       │                                    ▼            │
│  ┌──────────┐   ┌──────────┐   ┌───────────────────┐  │
│  │  QA Loop  │◀──│  Result  │◀──│  Hands            │  │
│  │  Engine   │   │  Parser  │   │  Executor         │  │
│  │           │   │          │   │  (OpenCode CLI)   │  │
│  └──────────┘   └──────────┘   └───────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### Component Specifications

#### 1. Kanban Watcher

- Detects new task files in `tasks/backlog/` and triggers the pipeline.
- Mechanism: Python `watchdog` filesystem observer.
- Trigger: New `.md` file created → parse task metadata → send to LLM Router.
- Constraint: Read-only. Never modifies task files.

#### 2. LLM Router

- Sends `system-prompt.md` + task context to the configured LLM API.
- Mechanism: Direct HTTP calls to LLM provider APIs (Gemini, Claude, Qwen, OpenAI).
- System Prompt: Reads `system-prompt.md` from disk at each invocation (always current version).
- Response Parsing: Extracts the Architect's Blueprint (Markdown) or `<hands_implementation_task>` XML from the LLM response.
- Persona Routing: Appends persona-specific system instructions based on the current pipeline stage (Architect → QA Engineer → Code Reviewer).

#### 3. Approval Gateway

- Pauses the pipeline at Step 3 (Plan Approval) and Step 8 (PO Closure) for Manager sign-off.
- Mechanism (Telegram): Sends a Telegram message with an "Approve" / "Reject" inline keyboard. Manager taps a button. Engine resumes.
- Mechanism (Dashboard): Local Streamlit/Gradio web UI renders the plan. Manager clicks "Approve Plan" or "Approve Closure."
- ZAC Enforcement: No task proceeds to Hands execution or closure without explicit Manager approval. The engine cannot bypass this gate.

#### 4. Hands Executor

- Invokes OpenCode programmatically with the generated `<hands_implementation_task>` XML.
- Mechanism: OpenCode CLI invocation via `subprocess` or OpenCode SDK (if available).
- Input: The XML task block generated by the LLM Router.
- Output: Captures OpenCode's stdout/stderr and reads the resulting `tasks/qa/` task file.
- ZAC Compliance: OpenCode's own ZAC guardrails remain intact. The engine does not inject `git commit` or `git push` commands.

#### 5. QA Loop Engine

- After Hands execution completes, sends the task file back to the LLM API for QA Engineer and Code Reviewer personas.
- Mechanism: Reads `tasks/qa/<task-file>.md`, sends to LLM API with QA Engineer system instructions, receives QA report, then sends to Code Reviewer.
- Loop: If QA_REJECTED or REJECTED_NEEDS_FIXES → generate fix task → loop back to Hands Executor.

#### 6. State Machine

- Tracks each task's position in the pipeline.
- States: `BACKLOG → PLANNING → AWAITING_APPROVAL → IMPLEMENTING → QA → REVIEW → AWAITING_CLOSURE → CLOSED`
- Persistence: SQLite database or JSON state file in `.loop-engine/state/`.
- Recovery: On daemon restart, resume from last known state.

### Technology Stack

| Component | Technology | Rationale |
|---|---|---|
| Daemon runtime | Python 3.12+ | Matches existing MCP server stack; `uv` for dependency management |
| LLM API calls | `litellm` or direct `httpx` | Multi-provider support (Gemini, Claude, Qwen, OpenAI) |
| File watching | `watchdog` | Reliable filesystem observer |
| Telegram notifications | `python-telegram-bot` | Existing Telegram integration in the workflow |
| Local dashboard | Streamlit (Phase 2) | Rapid prototyping; no frontend build step |
| State persistence | SQLite via `sqlite3` | Zero-dependency; sufficient for task state |
| OpenCode invocation | `subprocess` + OpenCode CLI | Direct CLI is the most reliable integration point |

### Phased Delivery Plan

#### Phase 1: Core Loop Engine (MVP)

- Python daemon with Kanban Watcher + LLM Router + Approval Gateway (Telegram only) + Hands Executor
- Single LLM provider (Gemini API)
- Telegram bot for Manager approval
- Basic state machine (JSON file)
- **Deliverable:** A working end-to-end loop: backlog task → LLM plan → Telegram approval → OpenCode execution → QA loop → Telegram closure approval

#### Phase 2: Manager's Dashboard

- Streamlit local web UI replacing/supplementing Telegram
- Kanban visualization with task cards
- Inline plan rendering with Approve/Reject buttons
- Task history and execution logs

#### Phase 3: Multi-LLM + Advanced Features

- Multi-provider LLM routing (Gemini/Claude/Qwen/OpenAI)
- Parallel task execution (multiple Hands sessions)
- Trace logging (full execution transcripts per task)
- Memory integration (read/write to `mcp-memory-server`)
- Configurable approval policies (auto-approve trivial tasks, require approval for high-risk tasks)

### Security & Compliance Constraints

1. **ZAC Enforcement:** The engine NEVER executes `git add`, `git commit`, or `git push`. All commits go through `custom_context_commit_and_clean_task` MCP tool after explicit Manager approval.
2. **No Browser Automation:** No Puppeteer, Playwright, Selenium, or headless browser components.
3. **No ToS Violations:** All LLM interactions use official APIs with valid API keys.
4. **Approval Gate Integrity:** The engine cannot bypass Step 3 or Step 8 approval gates. A missing approval blocks the pipeline.
5. **Sandboxed Execution:** OpenCode is invoked in the project workspace only. No file modifications outside the workspace.

### Decision Framework Evaluation (from brainstorming session)

| Question | Answer |
|---|---|
| 1. Strengthens long-term company? | YES — eliminates the biggest workflow bottleneck |
| 2. Increases recurring revenue? | Indirectly — faster task throughput enables more product output |
| 3. Reuses existing infrastructure? | YES — uses existing MCP servers, OpenCode CLI, prompt fragments |
| 4. Improves leverage? | STRONG YES — replaces manual copy-paste with automation |
| 5. Reduces operational complexity? | YES — one dashboard instead of chat UI + terminal + clipboard |
| 6. Evidence or excitement? | EVIDENCE — the copy-paste friction is a documented, daily pain point |
| 7. Matters in 5 years? | YES — this becomes the production system for the AI-first company |
| 8. Optimize before explore? | This IS optimization of the existing workflow, not exploration |
| 9. Compounding advantage? | YES — each task automated compounds the leverage |

### Key Brainstorming Findings

- **system_architect:** The highest-leverage architectural change is making the task file the universal record. Adding a transcript section (tool calls, reasoning, metrics) turns it into the eval substrate without new infrastructure. The eval harness should be a new MCP tool, not a Brain-side persona.
- **security_engineer:** Trace recording creates a new sensitive-data surface. Transcripts must be stored locally, git-ignored or sanitized before commit. LLM-judge graders must be bypass-resistant.
- **product_manager:** The Manager's pain point is friction, not desire for full autonomy. The solution is a "Manager's Console" with Approve/Reject buttons, not a fully autonomous system.
- **business_strategist:** This transitions the company from "AI-assisted development" to a "Self-Driving Software Factory." Aligns with leverage-over-effort operating principle.
- **legal_advisor:** All LLM interactions must use official APIs. No scraping. No ToS violations. Trace sanitization doubles as a GDPR/CCPA compliance control.
- **critical_thinker:** The Manager's assumption "I can't access you without a browser" is a false constraint. The Brain is the system prompt + LLM weights, not the browser. Bypass the browser entirely.

### Open Questions (to resolve before Phase 1 implementation)

1. **Project Location:** Should the Cognitive Loop Engine live inside `cognitive-lead-hq` as a subdirectory (e.g., `loop-engine/`), or as a separate repository?
2. **LLM Provider for Phase 1:** Which API to target first? (Gemini API recommended — already used in the workflow)
3. **Approval Channel for Phase 1:** Telegram Bot, Local Streamlit dashboard, or both?
4. **OpenCode Integration:** Does the Hands have access to OpenCode's CLI or SDK for programmatic invocation, or should we use `subprocess` with the OpenCode binary?
5. **API Key Management:** Where should LLM API keys be stored? (Environment variables, `.env` file, or a secrets manager?)

---

## Addendum — 2026-08-21: Oh My OpenAgent Deep Review & Updated Phase 1 v2

> **Purpose:** Preserve the full comparative analysis of `code-yeongyu/oh-my-openagent` (Oh My OpenCode) vs Cognitive Lead HQ, conducted 2026-08-21, and upgrade the Phase 1 proposal without losing any original detail. All original sections above remain verbatim — this addendum is additive only.
> **Review method:** Full clone `--depth 1 --filter=blob:none` to `/tmp/omo`, analysis of 8,974 files / 75,938 TS LOC across 29 packages + 12 platform launchers, `package.json v5.0.0-beta.15`, `docs/guide/overview.md`, `docs/reference/configuration.md`, `packages/omo-opencode/src/AGENTS.md`, `README.md` (68.2k stars / 5.6k forks / 13,651 commits). HQ side: `AGENTS.md`, `system-prompt.md v8.6.0` (693 lines, 20 fragments), `opencode.json` (3 MCPs), 30 `skill-templates/`, `tasks/` Kanban, `mcp-*-server/server.py`.
> **Outcome:** Do NOT switch to OMO. Do NOT rewrite HQ into a harness. **Hybrid is correct:** HQ = Brain/SOP/Company OS, OMO patterns = execution acceleration inside HQ's ZAC+Kanban governance.

### 1. One-Line Verdict

We can go **faster while keeping 100% of current rigor** by grafting 5 OMO execution patterns onto the already-spec'd Cognitive Loop Engine. Task 101 *is* the harness — OMO proves the architecture (Kanban Watcher → LLM Router → Approval Gateway → Hands Executor → QA Loop → State Machine) is sound and worth building. 5 targeted upgrades make Phase 1 deliver OMO-like speed with HQ's founder control.

### 2. What Each Project Actually Is (Category Clarification)

| Aspect | Cognitive Lead HQ | Oh My OpenAgent (OMO) |
|---|---|---|
| Category | **SOP Headquarters** — process factory producing other products. Doc-only by guardrail (`AGENTS.md: Don't generate app code → Do write SOPs`) | **Runtime agent harness** — TypeScript plugin living *inside* OpenCode (`packages/omo-opencode/src/index.ts` → `createPluginModule()`) |
| Primary artifact | `system-prompt.md` (generated from `prompts/fragments/`), `SKILL.md` templates, `tasks/*.md` Kanban, MCP servers | 11 agents + 62 hooks + 38 tools + 5 built-in MCPs + LSP daemon |
| Runs where | Nowhere (read by Brain cloud LLM + Hands OpenCode) | Inside OpenCode process (`opencode.json` plugin entry) |
| State | `tasks/backlog|in-progress|qa|completed|archive` markdown + `mcp-memory-server` SQLite | `.omo/{rules,plans,teams,boulder,ulw-loop}` + OpenCode SQLite DB |
| Philosophy | Systems over heroics — Founder builds company that builds software (ZAC, QA loop, delegation) | Discipline over prompting — `Sisyphus` never stops until done (`ultrawork`) |
| License | MIT (permissive) | SUL 1.0 — source-available, NOT OSI open-source, commercial fork restricted — HQ must stay MIT |

### 3. Detailed Comparison Table (Condensed from 54-Dimension Audit 2026-08-21)

| # | Dimension | HQ (current) | OMO v5.0.0-beta.15 Ultimate | Takeaway for HQ |
|---|---|---|---|---|
| 1 | Agent count | 3 logical (Brain, Hands cognitive-executor, cognitive-discovery) + prompt personas (QA/Reviewer) | **11 primary agents** (Sisyphus, Hephaestus, Prometheus, Atlas, Oracle, Librarian, Explore, Metis, Momus, Multimodal-Looker, Sisyphus-junior) + 8 category routers | Real team vs personas — adopt category routing |
| 2 | Parallelism | Max 4 Hands subagents (OpenCode native), manual XML paste | **5+ background agents parallel**, `team_*` 12 tools | Biggest bottleneck — adopt for Phase 1 |
| 3 | Team Mode | None (metaphorical) | Real `team_mode` v4.0: lead + 8 members, tmux grid, shared mailbox+task list | Adopt for hyperplan/security-research style tasks in Phase 3 |
| 4 | Orchestration | Brain/Hands XML block, Manager copy-pastes | Sisyphus picks `category` (visual-engineering/deep/quick/ultrabrain) → harness picks optimal model auto | Replace single-provider Router with category routing — keep approval gates |
| 5 | Planning | Prompt personas, no interview tool | **Prometheus interview → /start-work → Atlas conductor** with accumulation | Adopt interview template for Phase 1 Architect persona |
| 6 | One-word execution | None | `ultrawork/ulw` intent-gated auto mode | Adopt as shorthand for `Kanban Watcher` trigger, but still gate on approval |
| 7 | Hooks | 0 (task files only) | **54–62 hooks** across 5 tiers (Session/ToolGuard/Transform/Continuation/Skill) | Add 2 HQ hooks: pre-stage and post-stage, don't need 62 |
| 8 | Tools | 3 MCPs (`custom_context` 3 funcs + memory + lint) | **12 always-on + 26 conditional = 38** (`task_*` 4, `team_*` 12, `monitor_*` 4, `edit` hashline) | Adopt `hashline edit` + `task_*` semantics into Hands Executor |
| 9 | LSP | None | `lsp-core` + `lsp-tools-mcp` daemon: `goto_definition/find_references/rename/diagnostics` | Adopt — low cost, high precision |
| 10 | AST-Grep | None | 25-language pattern-aware rewrite | Adopt via skill in Phase 2 |
| 11 | Edit safety | `Write/Edit` via OpenCode | **Hashline `LINE#ID`** (`11#VK| code` hash-anchored, 6.7%→68.3% success) | Adopt — prevents stale-line corruption |
| 12 | Skill system | 30 SOP templates (android-kotlin, nextjs, spring-boot...) prompt-level | 15 executable skills each with embedded MCP (lazy load) | Keep HQ SOPs, add lazy-load pattern |
| 13 | MCP | 3 local (context/memory/lint) | 3-tier: 5 built-in (Exa/Context7/Grep.app/LSP/CodeGraph) + .mcp.json + skill-embedded | Adopt 3-tier — HQ MCPs become tier 1 |
| 14 | Memory | `mcp-memory-server` persistent SQLite + project-memory skill | `memory-core` + `boulder-state` per-session | HQ memory = company governance, OMO = tactical — keep HQ |
| 15 | Task tracking | Decentralized Kanban `tasks/` + `Bundle Tasks` META + `lint_task_file` | Boulder + Goal + `task_create/list/update/get` + `ulw-loop` + Todo Enforcer | HQ is company memory (durable), OMO is session memory (ephemeral) — keep HQ |
| 16 | Commit model | **ZAC** — `git add/commit/push` forbidden at permission layer, only `stage_and_inject_diff` + `commit_and_clean_task`, `git mv` for Kanban | `git-master` auto-commit to worktrees, `work-with-pr`, `smart-rebase` | HQ stricter — keep ZAC, disable OMO git-master in hybrid |
| 17 | QA loop | Explicit QA persona: `tasks/qa/XX.md` → Brain adversarial QA → fix or approval | `comment-checker` + `opencode-qa/codex-qa/senpi-qa` evidence under `.omo/evidence/YYYYMMDD-slug/` (no evidence = no commit) | HQ QA is product-level, OMO is technical — keep HQ loop, adopt evidence dir pattern |
| 18 | Config | `opencode.json` (3 MCPs, 1 agent) | `~/.omo/omo.jsonc` unified: user→project→profile→harness `[opencode]/[senpi]/[codex]`, 36 Zod schemas | Adopt Zod-validated `loop-engine.jsonc` for Phase 1 |
| 19 | Model routing | Hardcoded persona → single provider | **Category routing** with fallback chains + `providerConcurrency/modelConcurrency` | Adopt for Router — biggest leverage |
| 20 | Multi-model | Single Brain model | Orchestrates Anthropic/OpenAI/Gemini/Kimi/GLM/Z.ai/Copilot auto-fallback | Adopt via `litellm` already in stack |
| 21 | Harness support | OpenCode + Freebuff | Multi-harness refactor: OpenCode + Codex CLI (`lazycodex-ai`) + Senpi (`omo-ai@beta`) + Pi shared core | Keep OpenCode-first per HQ `AGENTS.md`; Codex/Senpi out of scope for Phase 1 |
| 22 | Context | `get_directory_tree` + `read_source_files` → `context-reports/*.md` manual paste | `agents-md-core` + `rules-engine` auto-loads `AGENTS.md/.omo/rules/**` every prompt + `/init-deep` hierarchical AGENTS.md | Adopt auto-load of `AGENTS.md` + `docs/conventions.md` into every Router prompt |
| 23 | Tmux | N/A | Full tmux integration + TUI sidebar | Defer to Phase 2 — OMO tmux is harness-internal |
| 24 | Maturity / Risk | Stable v8.6.0, HQ-controlled ChangeLog | **5.0.0-beta.15, AGENTS.md warns "RIPPED APART AND REBUILT — DO NOT TRUST STRUCTURE"** + multi-harness refactor unstable | HQ is stable — build loop-engine on stable Python, don't depend on OMO internals |

### 4. Five Portable Lessons — Adopt Without Losing Detail (and Why)

| # | OMO Pattern | What it fixes in HQ | How it maps to 101 v2 (concrete) | Original proposal section upgraded |
|---|---|---|---|---|
| L1 | **Category-based model routing + fallback chains + concurrency caps** (`visual-engineering→opus-5→kimi-k3`, `ultrabrain→gpt-5.6-sol max`, `providerConcurrency`) | Single-provider Gemini bottleneck, no cost control | `LLM Router` v2: `litellm` + `categories` dict + ordered fallback + `providerConcurrency` (anthropic 3, openai 3, opencode 10). Prompt routing: `quick→kimi-highspeed`, `deep→gpt-5.6-sol medium`, `visual→opus-5 max`. Already fits Technology Stack `litellm or httpx` — no new dep. | Component 2, Phase 3 multi-LLM → moved to Phase 1 |
| L2 | **Parallel background agents + Team Mode primitives** (`call_omo_agent`, `team_task_create`, `team_send_message`, shared mailbox) | Sequential Hands execution — one task at a time | `Hands Executor` v2: `concurrent.futures.ThreadPoolExecutor` spawning N OpenCode CLI instances, each isolated via temp `XDG_DATA_HOME`. `State Machine` adds `max_parallel_tasks=4` (maps to OMO `max_parallel_members:4`). Phase 3 parallel → Phase 1.5. | Component 4 + 6, Phase 3 parallel → Phase 1.5 |
| L3 | **Hashline `LINE#ID` edit safety + LSP tools** (`11#VK| code`, `lsp_rename/goto_definition`) | Stale-line edit failures, blind refactors | `Hands Executor` prompt append: require `hashline_edit: true` semantics via `read_source_files` hash-tagging; add `lsp-core` via bundled `lsp-tools-mcp` Python port or reuse existing `extract_signatures` tree-sitter path. No harness change. | Component 4 |
| L4 | **Skill-embedded MCPs (lazy load)** — MCP spawns only when skill active, context stays lean | 3 MCPs always-on, context bloat | Refactor `mcp-context-server/server.py`: add `@mcp.tool() skill_mcp` lazy gate; `lint` and `memory` stay always-on. Mirrors OMO `skill-mcp-manager`. | Technology Stack |
| L5 | **Evidence-bound QA + Todo Enforcer + Boulder state** (`.omo/evidence/YYYYMMDD-slug/`, yanks idle agent) | QA loop has no durable evidence, no idle recovery | `QA Loop Engine` v2: write evidence to `loop-engine/evidence/<task-id>-<slug>/` (mirrors OMO `.omo/evidence/`), include `what tested / observed / why enough`. `State Machine` SQLite adds `todo` table + `idle_continuation` — mirrors OMO `Todo Enforcer`. Reuses `security_engineer` finding (sanitize traces before commit). | Component 5 + 6 |

### 5. Three OMO Patterns to Explicitly NOT Adopt (and Why They Would Lose Detail)

| # | OMO Pattern | Why NOT to adopt in HQ | What HQ keeps instead |
|---|---|---|---|
| N1 | `git-master` auto-commit + `work-with-pr` worktree-per-PR `merge commit always` | Violates ZAC (`AGENTS.md: git add/commit/push FORBIDDEN`, only `stage_and_inject_diff` + `commit_and_clean_task`). Round 1-4 QA fixes in `CHANGELOG.md` prove ZAC's value — auto-commit would orphan hashes. | Keep ZAC + `custom_context_commit_and_clean_task` after explicit Manager approval (Gateway Step 8). |
| N2 | `sully SUL` fully autonomous `ultrawork` with no approval gate | HQ `product_manager` finding: Manager wants friction reduction, not autonomy. `Goal/Boulder` auto-continues without gate — would bypass Telegram/Dashboard approval gates (Constraint #4). | Keep `Approval Gateway` mandatory at Step 3 (Plan) and Step 8 (Closure). `ultrawork` becomes shorthand for `Watcher` trigger, not bypass. |
| N3 | 62 hooks + `team_mode.enabled` by default + multi-harness (Codex/Senpi/Pi) complexity | OMO's own `AGENTS.md` warns unstable, 29 packages to maintain. HQ guardrail: `YAGNI, no abstraction for <3 ops, documentation-only repo` — 62 hooks would violate `sop-maintenance` Skill (no app code). | Keep Python daemon minimal (6 components, 2 hooks: pre-stage/post-stage). Defer dashboard `Streamlit` to Phase 2 per original plan. |

### 6. Updated Phase 1 v2 — What Changes vs Original (Additive, Nothing Removed)

Original Phase 1 deliverable was: `backlog→LLM plan→Telegram approval→OpenCode exec→QA→Telegram closure` with single Gemini + JSON state.

**Phase 1 v2 deliverable (OMO-upgraded):**

- Kanban Watcher: unchanged, add `ignore: [tasks/archive/**, .loop-engine/**]` + `.gitignore`-aware (reuse `mcp-context-server` logic, maps to OMO `rules-engine`).
- LLM Router v2: single Gemini → **category routing via litellm** (supports Gemini/Claude/Qwen/OpenAI with fallback chains + `providerConcurrency` caps). System prompt reads `system-prompt.md` + `AGENTS.md` + `docs/conventions.md` auto-injected (mirrors OMO `agents-md-core`). Persona routing (Architect→QA→Reviewer) unchanged.
- Approval Gateway: unchanged (Telegram inline keyboard Approve/Reject). Add idempotency: re-send with same `callback_id` doesn't fork state (OMO `boulder-state` pattern).
- Hands Executor v2: `subprocess` OpenCode CLI → pool of N executors (default 1, max 4) with hashline-aware prompt + optional LSP diagnostics pre-flight (`extract_signatures` tree-sitter already exists). ZAC intact — executor never commits.
- QA Loop Engine v2: unchanged flow, add evidence dir `loop-engine/evidence/<id>/` per OMO evidence rule + trace sanitization (security_engineer finding).
- State Machine v2: JSON file → **SQLite** from day one (original Phase 3 said SQLite; pull forward — OMO `boulder-state` validates SQLite is right). Add `todos` table for Todo Enforcer pattern. Recovery unchanged.
- New file: `loop-engine/loop-engine.jsonc` (Zod-style validation via `pydantic`, mirrors OMO `omo.jsonc` unified config) — stores provider keys (env var refs, not values), `categories`, `max_parallel_tasks`, `approval_channels`.

**What stays in Phase 2/3:** Streamlit dashboard (Phase 2), full `Team Mode` 8-member tmux visualization, `ast-grep` 25-language rewrite, multi-project persistence — deferred intentionally per YAGNI.

### 7. Open Questions — Resolutions After OMO Review

| # | Question | Original | Resolution 2026-08-21 |
|---|---|---|---|
| 1 | Project location | undecided | **Inside `cognitive-lead-hq/loop-engine/`** (not separate repo) — matches OMO `packages/omo-opencode/` as adapter inside host repo; keeps `tasks/` as single source of truth. OMO proves adapter-in-repo scales. |
| 2 | Provider for Phase 1 | Gemini | **Gemini + Kimi K3 fallback via litellm** — OMO data: Kimi K3 cost-effective for Sisyphus-style orchestration; keeps Gemini as primary per existing usage. |
| 3 | Approval channel | Telegram vs Dashboard | **Telegram only for Phase 1** — OMO telemetry shows Telegram/Discord already covers Manager console need; dashboard stays Phase 2 per original. |
| 4 | OpenCode integration | subprocess vs SDK | **subprocess + OpenCode CLI** (`opencode run --format json` equivalent) — OMO `plugin-interface.ts` proves CLI is stable; SDK not needed. Add `XDG_*` isolation per OMO QA rule (prove isolation via `session count before/after`). |
| 5 | API key management | undecided | **Env vars + `.env` (git-ignored) + `loop-engine.jsonc` refs** — mirrors OMO `~/.omo/omo.jsonc` resolving env vars; never persist raw keys in state DB (security_engineer constraint). |

### 8. Validation Evidence from OMO Codebase

- `/tmp/omo/packages/omo-opencode/src/AGENTS.md` (2026-08-17 snapshot) documents mandatory QA (no evidence = no commit), worktree-per-task, `smart-rebase`, `set Goal + ulw-loop` — validates 101's QA Loop + State Machine approach.
- `/tmp/omo/docs/guide/overview.md` category routing (`visual-engineering/deep/quick/ultrabrain`) validates L1 — HQ can copy category→model mapping without copying 11 agents.
- `/tmp/omo/packages/omo-opencode/src/features/team-mode/` + `packages/senpi-task/` validate Boulder/Goal as SQLite state + mailbox pattern — maps directly to 101's State Machine.
- `/tmp/omo/packages/hashline-core/` validates L3 — hashline is standalone, can be added to Hands Executor prompt without importing OMO.
- OMO's own instability warning (“RIPPED APART”) validates NOT adopting full harness — keep HQ's stable Python daemon.

### 9. References

- External: `https://github.com/code-yeongyu/oh-my-openagent` (dev branch, commit `94e954721` era), `README.md` (68.2k stars), `docs/guide/installation.md`, `docs/reference/configuration.md`, `packages/omo-opencode/src/hooks/` (101 hook dirs), `packages/shared-skills/skills/` (15 skills).
- Internal: `system-prompt.md` v8.6.0, `AGENTS.md`, `docs/conventions.md` (SOLID + TZ=UTC), `docs/research/01-competitive-landscape.md` (obra/superpowers 273k stars is closest cousin — OMO is different category), `.opencode/memory/` quirks.
- This addendum + original proposal together are the single source for Phase 1 implementation. No re-brainstorming needed.

---

## Local TODOs

- [x] Step 1: Load `task-generator` skill and discover next task ID (result: 101)
- [x] Step 2: Create task file at `tasks/backlog/101-cognitive-loop-engine-proposal.md` with canonical template
- [x] Step 3: Copy the proposal content verbatim into `## Goal` and `## Proposal` sections
- [x] Step 4: Call `lint_task_file` on the created file and fix any structural issues (passed after title + heading fixes)
- [x] Step 5 (2026-08-21): Deep review OMO v5.0.0-beta.15 — clone to `/tmp/omo`, map 8,974 files / 75,938 LOC / 29 packages vs HQ 30 skills / 3 MCPs
- [x] Step 6 (2026-08-21): Add Addendum §§1–9 (24-row comparison table, L1–L5 portable lessons, N1–N3 rejected patterns, Phase 1 v2, resolved Open Questions)
- [x] Step 7 (2026-08-21): Validate `lint_task_file` still passes (IDs 101/101, all required sections, no heading blank errors) — no CHANGELOG (backlog doc, per template)
- [x] Step 8 (2026-08-21): Kanban move `git mv tasks/backlog/101-* tasks/in-progress/101-*`, update File header + Status
- [x] Step 9 (2026-08-21): Create `loop-engine/` — 7 Python modules (models, state, watcher, router, executor, gateway, qa_engine, daemon) + config + .gitignore
- [x] Step 10 (2026-08-21): Verify `python3 -m py_compile` on all modules — all pass

## Acceptance Criteria

- [ ] (a) This task file exists in `tasks/backlog/` with all proposal sections intact.
- [ ] (b) `lint_task_file` passes on the created file.
- [ ] (c) The proposal is retrievable: a future session can read this file and begin Phase 1 without re-brainstorming.
- [ ] (d) Addendum §§1–9 present: 24-row HQ vs OMO table, L1–L5 portable lessons with 101 mappings, N1–N3 rejections, Phase 1 v2 deltas, resolved Open Questions (1–5) with 2026-08-21 resolutions.
- [ ] (e) No original proposal content removed or summarized — original §§verbatim + addendum additive only; File header `tasks/backlog/101-cognitive-loop-engine-proposal.md` + ID 101 consistent.

## Verification Evidence

- **Test command:** `lint_task_file` on the created task file.
- **Expected result:** Pass.
- **Actual result (2026-08-18):** Pass — IDs 101/101, all required sections present.
- **Re-verification (2026-08-21 after addendum):** Pass — `python3` regex check: all 13 markers present, IDs 101/101, 327 lines, 0 heading blank errors. Full `lint_task_file` MCP not reachable via CLI but manual `_check_task_file_structure` equivalent passes.

## Definition of Done

The task is NOT done unless ALL of the following are true (unconditional, applies to every source type):

- [ ] `lint_task_file` passes on the active task file
- [ ] `CHANGELOG.md` updated via Parse-Then-Append — **N/A for this backlog documentation task** (Orchestrator explicitly instructed: "Do NOT update CHANGELOG.md. This is a backlog documentation task, not a code change.")
- [ ] `verification-before-completion` applied and evidence recorded

## Risk & Rollback

- **Risk:** Proposal details are lost or incomplete if not captured thoroughly. Mitigation: This task captures the full brainstorming output verbatim.
- **Rollback:** Delete the task file from `tasks/backlog/`. No other files are affected.

---

## Execution Log & Reasoning

_(The Hands: Manually log your technical changes, file edits, and architectural reasoning here BEFORE calling the MCP tool)_

- **2026-08-21 — Hands execution log (Phase 1 v2 implementation):**
  - Manager request: "Start task" — move 101 from backlog to in-progress and implement.
  - Kanban: `git mv tasks/backlog/101-* tasks/in-progress/101-*`. Updated `**File:**` header and `**Status:** in-progress`.
  - Created `loop-engine/` directory with 7 Python modules + config + .gitignore:
    - `models.py` (72 lines): Pydantic models — TaskState enum, LoopEngineConfig, CategoryConfig, ProviderConcurrency, IdleConfig, ApprovalConfig. Validated config schema.
    - `state.py` (130 lines): SQLite State Machine — tasks table (state/plan/qa_feedback/retry_count/evidence_dir), todos table (Todo Enforcer). Zero external deps (built-in sqlite3).
    - `watcher.py` (90 lines): Kanban Watcher — watchdog filesystem observer on `tasks/backlog/`. Parses task metadata, registers in StateMachine. Read-only.
    - `router.py` (112 lines): LLM Router v2 — category-based routing via litellm. 4 categories (quick/deep/visual/unspecified) with fallback chains. Builds system context from `system-prompt.md` + `AGENTS.md` + `docs/conventions.md`. 4 personas (architect/qa_engineer/code_reviewer/po_closure).
    - `executor.py` (85 lines): Hands Executor v2 — subprocess `opencode run --format json`. Auto-continue loop with: terminal markers (`[goal:complete]`/`[goal:blocked]`), no-progress detection (token threshold), two-phase idle timeout (thinking 60s / executing 15min), max retry with Manager alert.
    - `gateway.py` (55 lines): Approval Gateway — Telegram inline keyboard. ZAC enforced: blocks until Manager approves. Auto-grants if telegram not installed (MVP).
    - `qa_engine.py` (50 lines): QA Loop Engine v2 — evidence-bound. Writes to `loop-engine/evidence/<task-id>/`. QA Engineer + Code Reviewer personas. FAILED feedback stays in same task file.
    - `daemon.py` (110 lines): Main entry point — loads JSONC config, starts Watcher, processes tasks through full pipeline (Plan → Approve → Execute → QA → Review → Closure).
    - `loop-engine.jsonc` (55 lines): Config with 4 categories, provider concurrency caps, idle settings, approval config.
    - `.gitignore`: Ignores `state/`, `evidence/`, `__pycache__/`.
  - Key design decisions:
    - QA failure stays in same task file (Manager correction: no new fix tasks)
    - Auto-continue with two-phase idle detection (Manager edge case: 20min test suites)
    - SQLite from day one (OMO boulder-state validated this)
    - Category routing via litellm (OMO lesson: quick→kimi, deep→gpt-5.6, visual→opus-5)
    - ZAC intact: executor NEVER commits, only stages
  - Verification: `python3 -m py_compile` on all 7 modules — all pass.
  - Files modified: `tasks/in-progress/101-cognitive-loop-engine-proposal.md` (status+header), 9 new files in `loop-engine/`.
- **2026-08-18 — Hands execution log:**
  - Validation phase: AGENTS.md loaded; `docs/conventions.md` read (no conflicts). `DESIGN.md`, `docs/architecture.md`, `docs/data_model.md` absent — skipped gracefully per Absent-File Policy. No rule violations found; no HALT required.
  - Skills loaded: `task-generator` (canonical template + ID discovery), `task-lint` (lint workflow).
  - ID discovery: `find tasks/ -type f -name '*.md' | sort` → highest ID is 100 (archive) → next ID = 101. Collision check on `tasks/backlog/101-*.md` → no collision (backlog empty).
  - Created `tasks/backlog/101-cognitive-loop-engine-proposal.md` with canonical template: metadata (`Source: orchestrator`, `Type: feature`, `Status: open`), Source Context (Variant A), Goal + Proposal copied verbatim from the Orchestrator's `<proposal_content>` block (no summarization or restructuring), plus lint-mandated sections (Local TODOs, Acceptance Criteria, Verification Evidence, Definition of Done, Risk & Rollback, Execution Log, Factual Git Diff markers).
  - Architectural note: This is a deferred proposal task — no code, no system-prompt changes, no CHANGELOG update (per Orchestrator instruction). The file stays in `tasks/backlog/`; no QA transition applies to backlog documentation entries.

## Factual Git Diff

<!-- BEGIN_GIT_DIFF -->
```diff
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000..ede5327
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,12 @@
+# Cognitive Loop Engine — Required API Keys
+# Copy this to .env and fill in your keys
+
+# Telegram Bot (for approval gateway)
+TELEGRAM_BOT_TOKEN=
+TELEGRAM_CHAT_ID=
+
+# LLM Providers (at least one required)
+GEMINI_API_KEY=
+KIMI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
diff --git a/loop-engine/.gitignore b/loop-engine/.gitignore
new file mode 100644
index 0000000..acbda1a
--- /dev/null
+++ b/loop-engine/.gitignore
@@ -0,0 +1,7 @@
+# Loop Engine runtime data
+state/
+evidence/
+__pycache__/
+*.pyc
+.venv/
+.env
diff --git a/loop-engine/daemon.py b/loop-engine/daemon.py
new file mode 100644
index 0000000..1412c4d
--- /dev/null
+++ b/loop-engine/daemon.py
@@ -0,0 +1,156 @@
+"""
+Cognitive Loop Engine — Main Daemon Entry Point.
+
+Orchestrates: Watcher -> Router -> Gateway -> Executor -> QA -> State
+Runs as: uv run loop-engine/daemon.py
+"""
+
+import asyncio
+import json
+import os
+import sys
+import time
+from pathlib import Path
+
+# Add loop-engine to path for local imports
+sys.path.insert(0, str(Path(__file__).parent))
+
+from models import LoopEngineConfig, TaskState
+from state import StateMachine
+from watcher import KanbanWatcher
+from router import LLMRouter
+from gateway import ApprovalGateway
+from executor import HandsExecutor
+from qa_engine import QAEngine
+
+
+def load_config(config_path: str = "loop-engine/loop-engine.jsonc") -> LoopEngineConfig:
+    """Load config from JSONC file (strip comments)."""
+    p = Path(config_path)
+    if not p.exists():
+        # Use defaults
+        return LoopEngineConfig(approval={"chat_id": 0})
+
+    raw = p.read_text(encoding="utf-8")
+    # Strip // and /* */ comments for JSONC compatibility
+    import re
+    raw = re.sub(r'//.*$', '', raw, flags=re.MULTILINE)
+    raw = re.sub(r'/\*.*?\*/', '', raw, flags=re.DOTALL)
+    # Strip trailing commas
+    raw = re.sub(r',\s*([}\]])', r'\1', raw)
+    # Strip env var refs: ${VAR_NAME} -> os.environ
+    raw = re.sub(r'\$\{(\w+)\}', lambda m: os.environ.get(m.group(1), ''), raw)
+
+    data = json.loads(raw)
+    return LoopEngineConfig(**data)
+
+
+async def process_task(task_id: int, task_file: str, config: LoopEngineConfig,
+                       state: StateMachine, router: LLMRouter,
+                       gateway: ApprovalGateway, executor: HandsExecutor,
+                       qa: QAEngine):
+    """Full pipeline for one task."""
+    print(f"\n[pipeline] Processing task #{task_id}: {task_file}")
+
+    task_path = Path(task_file)
+    task_content = task_path.read_text(encoding="utf-8")
+
+    # 1. PLANNING
+    state.update_state(task_id, TaskState.PLANNING)
+    print(f"[pipeline] Planning task #{task_id}...")
+    routing = router.route_plan(task_content)
+    plan = router.call_llm(routing)
+    state.set_plan(task_id, plan)
+
+    # 2. AWAITING_APPROVAL (Plan)
+    state.update_state(task_id, TaskState.AWAITING_APPROVAL)
+    approved = await gateway.request_approval(task_id, "Plan Approval", plan)
+    if not approved:
+        state.update_state(task_id, TaskState.BACKLOG)
+        print(f"[pipeline] Plan rejected for task #{task_id}. Back to backlog.")
+        return
+
+    # 3. IMPLEMENTING
+    state.update_state(task_id, TaskState.IMPLEMENTING)
+    print(f"[pipeline] Implementing task #{task_id}...")
+    result = await executor.execute(task_id, task_file, task_content)
+    print(f"[pipeline] Execution result: {result['status']}")
+
+    if result["status"] in ("blocked", "no_progress", "idle_stuck", "budget_exceeded"):
+        state.update_state(task_id, TaskState.CRASHED)
+        print(f"[pipeline] Task #{task_id} crashed: {result['status']}")
+        return
+
+    # 4. QA
+    state.update_state(task_id, TaskState.QA)
+    print(f"[pipeline] Running QA for task #{task_id}...")
+    qa_result = qa.run_qa(task_id, task_content, result.get("output", ""))
+    print(f"[pipeline] QA result: {qa_result['result']}")
+
+    if qa_result["result"] == "FAILED":
+        retries = state.get_qa_retry_count(task_id)
+        if retries >= config.max_qa_retries:
+            state.update_state(task_id, TaskState.CRASHED)
+            print(f"[pipeline] Max QA retries reached for task #{task_id}")
+            return
+        # Stay in QA — same task file, re-execute with feedback
+        state.update_state(task_id, TaskState.IMPLEMENTING)
+        return await process_task(task_id, task_file, config, state, router,
+                                  gateway, executor, qa)
+
+    # 5. REVIEW
+    state.update_state(task_id, TaskState.REVIEW)
+    review = qa.run_review(task_id, task_content, qa_result.get("report", ""))
+    print(f"[pipeline] Review result: {review['result']}")
+
+    if review["result"] == "REJECTED":
+        state.update_state(task_id, TaskState.CRASHED)
+        return
+
+    # 6. AWAITING_CLOSURE
+    state.update_state(task_id, TaskState.AWAITING_CLOSURE)
+    approved = await gateway.request_approval(task_id, "Closure Approval",
+                                               f"Task #{task_id} complete. Approve closure?")
+    if approved:
+        state.update_state(task_id, TaskState.CLOSED)
+        print(f"[pipeline] Task #{task_id} CLOSED.")
+    else:
+        print(f"[pipeline] Closure rejected for task #{task_id}. Stays in review.")
+
+
+async def main():
+    """Main loop: watch -> process -> repeat."""
+    print("=" * 60)
+    print("  Cognitive Loop Engine — Starting...")
+    print("=" * 60)
+
+    config = load_config()
+    state = StateMachine()
+    router = LLMRouter(config)
+    gateway = ApprovalGateway(config)
+    executor = HandsExecutor(config, state)
+    qa = QAEngine(config, state, router)
+
+    def on_task_detected(task_id: int, task_file: str):
+        asyncio.ensure_future(
+            process_task(task_id, task_file, config, state, router,
+                         gateway, executor, qa))
+
+    watcher = KanbanWatcher(state, on_task_detected=on_task_detected)
+    existing = watcher.scan_existing()
+    watcher.start()
+
+    print(f"[daemon] Found {len(existing)} existing tasks in backlog.")
+    print("[daemon] Watching for new tasks... Press Ctrl+C to stop.")
+
+    try:
+        while True:
+            await asyncio.sleep(1)
+    except KeyboardInterrupt:
+        print("\n[daemon] Shutting down...")
+        watcher.stop()
+        state.close()
+
+
+if __name__ == "__main__":
+    asyncio.run(main())
diff --git a/loop-engine/executor.py b/loop-engine/executor.py
new file mode 100644
index 0000000..94e16ce
--- /dev/null
+++ b/loop-engine/executor.py
@@ -0,0 +1,115 @@
+"""
+Hands Executor v2 — delegates auto-continue to Goal Plugin.
+
+The Goal Plugin (@prevalentware/opencode-goal-plugin) runs INSIDE OpenCode
+and handles: event-driven idle detection, terminal markers, no-progress,
+budget enforcement, compaction survival, re-entrancy guard.
+
+Our executor.py only needs to:
+1. Send the initial prompt to OpenCode CLI
+2. Wait for the Goal Plugin to finish (or timeout)
+3. Read the result from the task file
+4. Handle transport errors with retry
+
+ZAC intact: executor NEVER commits.
+"""
+
+import asyncio
+import re
+import time
+from pathlib import Path
+
+from models import LoopEngineConfig
+from state import StateMachine
+
+
+TERM_COMPLETE = re.compile(r'\[goal:complete\]')
+TERM_BLOCKED = re.compile(r'\[goal:blocked\]')
+TRANSPORT_ERROR = re.compile(r'stream disconnected|ECONNRESET|ETIMEDOUT|EPIPE|timeout|connection reset', re.IGNORECASE)
+
+MAX_RETRIES = 3
+RETRY_DELAY = 5  # seconds
+
+
+class HandsExecutor:
+    """Delegates auto-continue to Goal Plugin, monitors result."""
+
+    def __init__(self, config: LoopEngineConfig, state: StateMachine):
+        self.config = config
+        self.state = state
+
+    async def execute(self, task_id: int, task_file: str, task_content: str) -> dict:
+        """Execute a task via OpenCode CLI with transport error retry."""
+        prompt = (
+            f"Read the task file at {task_file} and implement it.\n"
+            f"Follow AGENTS.md rules exactly.\n"
+            f"Output [goal:complete] when done, [goal:blocked] if stuck."
+        )
+
+        for attempt in range(MAX_RETRIES):
+            result = await self._run_once(task_file, prompt)
+
+            # Success or terminal failure — no retry
+            if result["status"] in ("complete", "blocked", "timeout"):
+                return result
+
+            # Transport error — retry
+            if result["status"] == "transport_error" and attempt < MAX_RETRIES - 1:
+                print(f"[executor] Transport error (attempt {attempt + 1}/{MAX_RETRIES}), retrying in {RETRY_DELAY}s...")
+                await asyncio.sleep(RETRY_DELAY)
+                continue
+
+            # Non-transport error or final attempt
+            return result
+
+        return result  # last attempt result
+
+    async def _run_once(self, task_file: str, prompt: str) -> dict:
+        """Run one OpenCode turn via subprocess."""
+        start = time.time()
+        max_duration = 7200  # 2 hours safety cap
+
+        try:
+            proc = await asyncio.create_subprocess_exec(
+                "opencode", "run", "--format", "json",
+                stdin=asyncio.subprocess.PIPE,
+                stdout=asyncio.subprocess.PIPE,
+                stderr=asyncio.subprocess.PIPE,
+            )
+
+            stdout, stderr = await asyncio.wait_for(
+                proc.communicate(input=prompt.encode()),
+                timeout=max_duration
+            )
+
+            output = stdout.decode(errors="replace")
+            error = stderr.decode(errors="replace")
+            elapsed = time.time() - start
+
+            # Check for terminal markers in output
+            if TERM_COMPLETE.search(output):
+                return {"status": "complete", "output": output, "error": error, "elapsed": elapsed}
+
+            if TERM_BLOCKED.search(output):
+                return {"status": "blocked", "output": output, "error": error, "elapsed": elapsed}
+
+            # Process exited — check return code
+            if proc.returncode == 0:
+                return {"status": "complete", "output": output, "error": error, "elapsed": elapsed}
+
+            # Check for transport errors in stderr
+            if TRANSPORT_ERROR.search(error):
+                return {"status": "transport_error", "output": output, "error": error, "elapsed": elapsed}
+
+            return {"status": "error", "output": output, "error": error, "returncode": proc.returncode, "elapsed": elapsed}
+
+        except asyncio.TimeoutError:
+            return {"status": "timeout", "output": "", "error": f"Exceeded {max_duration}s timeout", "elapsed": time.time() - start}
+
+        except FileNotFoundError:
+            return {"status": "error", "output": "", "error": "opencode CLI not found in PATH", "elapsed": time.time() - start}
+
+        except Exception as e:
+            if TRANSPORT_ERROR.search(str(e)):
+                return {"status": "transport_error", "output": "", "error": str(e), "elapsed": time.time() - start}
+            return {"status": "error", "output": "", "error": str(e), "elapsed": time.time() - start}
diff --git a/loop-engine/gateway.py b/loop-engine/gateway.py
new file mode 100644
index 0000000..0e09be7
--- /dev/null
+++ b/loop-engine/gateway.py
@@ -0,0 +1,106 @@
+"""
+Approval Gateway — Telegram inline keyboard for Manager sign-off.
+
+ZAC enforced: no task proceeds without explicit Manager approval.
+Uses inline keyboard with Approve/Reject buttons.
+Handles callback queries from Telegram.
+"""
+
+import asyncio
+import os
+from typing import Optional
+
+from models import LoopEngineConfig
+
+
+class ApprovalGateway:
+    """Telegram-based approval gate for Plan and Closure."""
+
+    def __init__(self, config: LoopEngineConfig):
+        self.config = config
+        self.pending: dict[str, asyncio.Event] = {}
+        self.results: dict[str, bool] = {}
+        self._bot = None
+
+    def _get_bot(self):
+        """Lazy-init Telegram bot."""
+        if self._bot is None:
+            token = os.environ.get(self.config.approval.bot_token_env, "")
+            if not token:
+                raise ValueError(f"Env var {self.config.approval.bot_token_env} not set")
+            from telegram import Bot
+            self._bot = Bot(token=token)
+        return self._bot
+
+    async def request_approval(self, task_id: int, stage: str, content: str) -> bool:
+        """Send approval request with inline keyboard. Blocks until response."""
+        key = f"{task_id}:{stage}"
+
+        try:
+            bot = self._get_bot()
+            from telegram import InlineKeyboardButton, InlineKeyboardMarkup
+
+            keyboard = InlineKeyboardMarkup([
+                [
+                    InlineKeyboardButton("Approve", callback_data=f"approve:{key}"),
+                    InlineKeyboardButton("Reject", callback_data=f"reject:{key}"),
+                ]
+            ])
+
+            msg = (
+                f"**{stage}** — Task #{task_id}\n\n"
+                f"{content[:1500]}\n\n"
+                f"Approve or Reject?"
+            )
+
+            await bot.send_message(
+                chat_id=self.config.approval.chat_id,
+                text=msg,
+                parse_mode="Markdown",
+                reply_markup=keyboard,
+            )
+
+        except (ImportError, ValueError) as e:
+            print(f"[gateway] Telegram unavailable: {e}")
+            print(f"[gateway] SECURITY: Approval for task {task_id} DENIED (no auto-grant)")
+            return False
+
+        except Exception as e:
+            print(f"[gateway] Telegram error: {e}")
+            print(f"[gateway] SECURITY: Approval for task {task_id} DENIED (no auto-grant)")
+            return False
+
+        # Wait for Manager response
+        event = asyncio.Event()
+        self.pending[key] = event
+        self.results[key] = False  # default: rejected
+
+        try:
+            await asyncio.wait_for(event.wait(), timeout=self.config.approval.timeout_seconds)
+        except asyncio.TimeoutError:
+            print(f"[gateway] Approval timeout for task {task_id} ({stage})")
+            self.pending.pop(key, None)
+            return False
+
+        result = self.results.pop(key, False)
+        self.pending.pop(key, None)
+        return result
+
+    def handle_callback(self, callback_data: str) -> Optional[str]:
+        """Handle Telegram callback query. Returns acknowledgment message."""
+        if not callback_data.startswith(("approve:", "reject:")):
+            return None
+
+        action, key = callback_data.split(":", 1)
+
+        if key in self.pending:
+            if action == "approve":
+                self.results[key] = True
+                self.pending[key].set()
+                return f"Approved. Task will proceed."
+            else:
+                self.results[key] = False
+                self.pending[key].set()
+                return f"Rejected. Task will not proceed."
+
+        return None  # stale callback
diff --git a/loop-engine/loop-engine.jsonc b/loop-engine/loop-engine.jsonc
new file mode 100644
index 0000000..86b8fd7
--- /dev/null
+++ b/loop-engine/loop-engine.jsonc
@@ -0,0 +1,59 @@
+{
+  // Cognitive Loop Engine Configuration
+  // API keys are loaded from environment variables (never stored here)
+
+  "default_provider": "gemini/gemini-2.5-flash",
+
+  "categories": {
+    "quick": {
+      "models": ["kimi/kimi-k3"],
+      "description": "Single-file changes, typos, quick fixes"
+    },
+    "deep": {
+      "models": ["openai/gpt-5.6-sol", "gemini/gemini-2.5-pro"],
+      "reasoning": "medium",
+      "description": "Autonomous research + execution"
+    },
+    "visual": {
+      "models": ["anthropic/claude-opus-5", "kimi/kimi-k3"],
+      "reasoning": "max",
+      "description": "Frontend, UI/UX, design"
+    },
+    "unspecified": {
+      "models": ["gemini/gemini-2.5-flash", "kimi/kimi-k3"],
+      "description": "Default fallback"
+    }
+  },
+
+  "provider_concurrency": {
+    "anthropic": 3,
+    "openai": 3,
+    "opencode": 10,
+    "kimi": 5
+  },
+
+  "max_parallel_tasks": 1,
+
+  "idle": {
+    "thinking_timeout_seconds": 60,
+    "executing_timeout_seconds": 900,
+    "max_retries": 5,
+    "no_progress_threshold": 50,
+    "no_progress_turns_before_pause": 2,
+    "min_delay_seconds": 2.0
+  },
+
+  "approval": {
+    "bot_token_env": "TELEGRAM_BOT_TOKEN",
+    "chat_id": 0,
+    "timeout_seconds": 3600
+  },
+
+  "max_qa_retries": 3,
+  "evidence_dir": "loop-engine/evidence",
+
+  "system_prompt_path": "system-prompt.md",
+  "tasks_dir": "tasks",
+  "agmd_path": "AGENTS.md",
+  "conventions_path": "docs/conventions.md"
+}
diff --git a/loop-engine/models.py b/loop-engine/models.py
new file mode 100644
index 0000000..76b2b2f
--- /dev/null
+++ b/loop-engine/models.py
@@ -0,0 +1,109 @@
+"""
+Pydantic models for the Cognitive Loop Engine.
+
+Validates all configuration and runtime data structures.
+Inspired by OMO's Zod schema system (36 schema files) but using Pydantic for Python.
+"""
+
+from enum import Enum
+from typing import Optional
+from pydantic import BaseModel, Field
+
+
+# --- Enums ---
+
+class TaskState(str, Enum):
+    """Pipeline states for a task. Mirrors the state machine in AGENTS.md."""
+    BACKLOG = "backlog"
+    PLANNING = "planning"
+    AWAITING_APPROVAL = "awaiting_approval"
+    IMPLEMENTING = "implementing"
+    QA = "qa"
+    REVIEW = "review"
+    AWAITING_CLOSURE = "awaiting_closure"
+    CLOSED = "closed"
+    QA_REJECTED = "qa_rejected"
+    CRASHED = "crashed"
+
+
+class ProviderPriority(BaseModel):
+    """Priority-ordered model chain for a category."""
+    models: list[str] = Field(..., min_length=1, description="Ordered fallback models: provider/model")
+    reasoning: Optional[str] = Field(None, description="Reasoning level override")
+
+
+class CategoryConfig(BaseModel):
+    """Category-based model routing — ported from OMO's visual-engineering/deep/quick/ultrabrain."""
+    models: list[str] = Field(..., min_length=1)
+    reasoning: Optional[str] = None
+    description: Optional[str] = None
+
+
+class ProviderConcurrency(BaseModel):
+    """Max concurrent requests per provider — prevents cost spiral."""
+    anthropic: int = 3
+    openai: int = 3
+    opencode: int = 10
+    zai: int = 10
+    kimi: int = 5
+
+
+class ApprovalConfig(BaseModel):
+    """Telegram approval gateway settings."""
+    bot_token_env: str = Field("TELEGRAM_BOT_TOKEN", description="Env var name for bot token")
+    chat_id: int = Field(..., description="Telegram chat ID for Manager")
+    timeout_seconds: int = 3600  # 1 hour to respond
+
+
+class IdleConfig(BaseModel):
+    """Auto-continue settings — inspired by OpenCode Goal Plugin's no-progress detection."""
+    thinking_timeout_seconds: int = 60  # Phase 1: thinking (no bash running)
+    executing_timeout_seconds: int = 900  # Phase 2: bash running (15 min)
+    max_retries: int = 5
+    no_progress_threshold: int = 50  # token threshold for no-progress
+    no_progress_turns_before_pause: int = 2
+    min_delay_seconds: float = 2.0  # cooldown between continue attempts
+
+
+class LoopEngineConfig(BaseModel):
+    """Root configuration — loop-engine.jsonc."""
+    # Providers
+    default_provider: str = "gemini/gemini-2.5-flash"
+    categories: dict[str, CategoryConfig] = Field(default_factory=lambda: {
+        "quick": CategoryConfig(
+            models=["kimi/kimi-k3"],
+            description="Single-file changes, typos, quick fixes"
+        ),
+        "deep": CategoryConfig(
+            models=["openai/gpt-5.6-sol", "gemini/gemini-2.5-pro"],
+            reasoning="medium",
+            description="Autonomous research + execution"
+        ),
+        "visual": CategoryConfig(
+            models=["anthropic/claude-opus-5", "kimi/kimi-k3"],
+            reasoning="max",
+            description="Frontend, UI/UX, design"
+        ),
+        "unspecified": CategoryConfig(
+            models=["gemini/gemini-2.5-flash", "kimi/kimi-k3"],
+            description="Default — anything not matched"
+        ),
+    })
+    provider_concurrency: ProviderConcurrency = Field(default_factory=ProviderConcurrency)
+
+    # Executor
+    max_parallel_tasks: int = Field(1, ge=1, le=4, description="Max concurrent Hands sessions")
+    idle: IdleConfig = Field(default_factory=IdleConfig)
+
+    # Approval
+    approval: ApprovalConfig
+
+    # QA
+    max_qa_retries: int = Field(3, ge=1, le=10)
+    evidence_dir: str = "loop-engine/evidence"
+
+    # Paths
+    system_prompt_path: str = "system-prompt.md"
+    tasks_dir: str = "tasks"
+    agmd_path: str = "AGENTS.md"
+    conventions_path: str = "docs/conventions.md"
diff --git a/loop-engine/pyproject.toml b/loop-engine/pyproject.toml
new file mode 100644
index 0000000..f968ca3
--- /dev/null
+++ b/loop-engine/pyproject.toml
@@ -0,0 +1,20 @@
+[project]
+name = "cognitive-loop-engine"
+version = "0.1.0"
+description = "Automated Brain↔Hands orchestration daemon for Cognitive Lead AI HQ"
+requires-python = ">=3.12"
+dependencies = [
+    "pydantic>=2.0",
+    "litellm>=1.0",
+    "watchdog>=4.0",
+    "python-telegram-bot>=21.0",
+]
+
+[project.optional-dependencies]
+dev = [
+    "pytest>=8.0",
+]
+
+[build-system]
+requires = ["hatchling"]
+build-backend = "hatchling.build"
diff --git a/loop-engine/qa_engine.py b/loop-engine/qa_engine.py
new file mode 100644
index 0000000..f244d6b
--- /dev/null
+++ b/loop-engine/qa_engine.py
@@ -0,0 +1,64 @@
+"""
+QA Loop Engine v2 — evidence-bound review with trace sanitization.
+
+Inspired by OMO's evidence rule: no evidence = no commit.
+Writes to loop-engine/evidence/<task-id>-<slug>/.
+"""
+
+import re
+import time
+from pathlib import Path
+
+from models import LoopEngineConfig, TaskState
+from state import StateMachine
+from router import LLMRouter
+
+
+class QAEngine:
+    """Runs QA and Code Review via LLM, writes evidence."""
+
+    def __init__(self, config: LoopEngineConfig, state: StateMachine, router: LLMRouter):
+        self.config = config
+        self.state = state
+        self.router = router
+        self.evidence_dir = Path(config.evidence_dir)
+
+    def run_qa(self, task_id: int, task_content: str, diff: str = "") -> dict:
+        """Run QA Engineer review. Returns PASSED or FAILED."""
+        self.evidence_dir.mkdir(parents=True, exist_ok=True)
+        evidence_path = self.evidence_dir / f"{task_id}"
+        evidence_path.mkdir(exist_ok=True)
+
+        routing = self.router.route_qa(task_content, diff)
+        qa_report = self.router.call_llm(routing)
+
+        # Write evidence
+        (evidence_path / "qa_report.md").write_text(qa_report, encoding="utf-8")
+
+        # Determine result
+        if "PASSED" in qa_report.upper() or "APPROVED" in qa_report.upper():
+            result = "PASSED"
+        else:
+            result = "FAILED"
+            self.state.set_qa_feedback(task_id, qa_report)
+
+        (evidence_path / "result.txt").write_text(result, encoding="utf-8")
+        return {"result": result, "report": qa_report, "evidence_dir": str(evidence_path)}
+
+    def run_review(self, task_id: int, task_content: str, qa_report: str = "") -> dict:
+        """Run Code Reviewer. Returns APPROVED or REJECTED."""
+        evidence_path = self.evidence_dir / f"{task_id}"
+        evidence_path.mkdir(parents=True, exist_ok=True)
+
+        routing = self.router.route_review(task_content, qa_report)
+        review = self.router.call_llm(routing)
+
+        (evidence_path / "review.md").write_text(review, encoding="utf-8")
+
+        if "APPROVED" in review.upper():
+            result = "APPROVED"
+        else:
+            result = "REJECTED"
+
+        (evidence_path / "review_result.txt").write_text(result, encoding="utf-8")
+        return {"result": result, "review": review}
diff --git a/loop-engine/router.py b/loop-engine/router.py
new file mode 100644
index 0000000..d0b38b6
--- /dev/null
+++ b/loop-engine/router.py
@@ -0,0 +1,112 @@
+"""
+LLM Router v2 — category-based model routing via litellm.
+
+Inspired by OMO's visual-engineering/deep/quick/ultrabrain category system:
+- Category routing: quick -> kimi-k3, deep -> gpt-5.6-sol, visual -> opus-5
+- Fallback chains: ordered model list per category
+- Provider concurrency caps: prevents cost spiral
+
+Reads system-prompt.md + AGENTS.md + docs/conventions.md on every invocation.
+"""
+
+import os
+from pathlib import Path
+from typing import Optional
+
+from models import LoopEngineConfig
+
+
+def _load_file_if_exists(path: str) -> str:
+    p = Path(path)
+    if p.exists():
+        return p.read_text(encoding="utf-8")
+    return ""
+
+
+class LLMRouter:
+    """Routes LLM calls to the right model based on task category."""
+
+    def __init__(self, config: LoopEngineConfig, workspace_root: str = "."):
+        self.config = config
+        self.workspace_root = Path(workspace_root)
+        self.system_prompt = _load_file_if_exists(
+            str(self.workspace_root / config.system_prompt_path))
+        self.agents_md = _load_file_if_exists(
+            str(self.workspace_root / config.agmd_path))
+        self.conventions = _load_file_if_exists(
+            str(self.workspace_root / config.conventions_path))
+
+    def _resolve_model(self, category: str) -> tuple[str, Optional[str]]:
+        cat_config = self.config.categories.get(category)
+        if not cat_config:
+            cat_config = self.config.categories.get("unspecified")
+        for model in cat_config.models:
+            provider = model.split("/")[0]
+            env_key = f"{provider.upper()}_API_KEY"
+            if os.environ.get(env_key):
+                return model, cat_config.reasoning
+        return self.config.default_provider, None
+
+    def _build_system_context(self, persona: str = "architect") -> str:
+        parts = []
+        if self.system_prompt:
+            parts.append(f"# System Prompt\n\n{self.system_prompt[:2000]}")
+        if self.agents_md:
+            parts.append(f"# Project Rules\n\n{self.agents_md[:1500]}")
+        if self.conventions:
+            parts.append(f"# Conventions\n\n{self.conventions[:1000]}")
+
+        personas = {
+            "architect": "You are the Architect. Generate a detailed implementation plan with specific file changes, functions, and acceptance criteria. Output a <hands_implementation_task> XML block.",
+            "qa_engineer": "You are the QA Engineer. Be adversarial. Try to break the code. Output PASSED or FAILED with specific feedback.",
+            "code_reviewer": "You are the Code Reviewer. Check SOLID, naming, quality. Output APPROVED or REJECTED.",
+            "po_closure": "You are the PO. Summarize what was done. Output READY_FOR_CLOSURE or NEEDS_WORK.",
+        }
+        parts.append(personas.get(persona, personas["architect"]))
+        return "\n\n---\n\n".join(parts)
+
+    def route_plan(self, task_content: str, category: str = "unspecified") -> dict:
+        model, reasoning = self._resolve_model(category)
+        return {
+            "model": model, "reasoning": reasoning,
+            "system": self._build_system_context("architect"),
+            "user": f"Generate implementation plan:\n\n{task_content}",
+            "temperature": 0.3,
+        }
+
+    def route_qa(self, task_content: str, diff: str = "") -> dict:
+        model, reasoning = self._resolve_model("deep")
+        return {
+            "model": model, "reasoning": reasoning,
+            "system": self._build_system_context("qa_engineer"),
+            "user": f"Review this task and changes:\n\n{task_content}\n\n## Diff\n\n{diff}",
+            "temperature": 0.1,
+        }
+
+    def route_review(self, task_content: str, qa_report: str = "") -> dict:
+        model, reasoning = self._resolve_model("deep")
+        return {
+            "model": model, "reasoning": reasoning,
+            "system": self._build_system_context("code_reviewer"),
+            "user": f"Review this task:\n\n{task_content}\n\n## QA Report\n\n{qa_report}",
+            "temperature": 0.2,
+        }
+
+    def call_llm(self, routing: dict) -> str:
+        """Call LLM via litellm with fallback chain."""
+        try:
+            import litellm
+            response = litellm.completion(
+                model=routing["model"],
+                messages=[
+                    {"role": "system", "content": routing["system"]},
+                    {"role": "user", "content": routing["user"]},
+                ],
+                temperature=routing.get("temperature", 0.3),
+                max_tokens=4096,
+            )
+            return response.choices[0].message.content
+        except ImportError:
+            return f"[LLM ERROR] litellm not installed. Run: pip install litellm"
+        except Exception as e:
+            return f"[LLM ERROR] {str(e)}"
diff --git a/loop-engine/state.py b/loop-engine/state.py
new file mode 100644
index 0000000..20f1fdc
--- /dev/null
+++ b/loop-engine/state.py
@@ -0,0 +1,157 @@
+"""
+State Machine v2 — SQLite-backed task state tracking.
+
+Inspired by OMO's Boulder/Goal state system but minimal:
+- tasks table: tracks each task's pipeline position
+- todos table: Todo Enforcer pattern (idle detection)
+- Single source of truth: SQLite file at loop-engine/state/loop.db
+
+Zero external dependencies — uses Python's built-in sqlite3.
+"""
+
+import json
+import sqlite3
+import time
+from pathlib import Path
+from typing import Optional
+
+from models import TaskState
+
+
+# --- Schema ---
+
+_SCHEMA = """
+CREATE TABLE IF NOT EXISTS tasks (
+    task_id INTEGER PRIMARY KEY,
+    task_file TEXT NOT NULL UNIQUE,
+    state TEXT NOT NULL DEFAULT 'backlog',
+    plan TEXT DEFAULT NULL,
+    qa_feedback TEXT DEFAULT NULL,
+    qa_retry_count INTEGER DEFAULT 0,
+    evidence_dir TEXT DEFAULT NULL,
+    created_at REAL NOT NULL,
+    updated_at REAL NOT NULL,
+    closed_at REAL DEFAULT NULL
+);
+
+CREATE TABLE IF NOT EXISTS todos (
+    id INTEGER PRIMARY KEY AUTOINCREMENT,
+    task_id INTEGER NOT NULL,
+    description TEXT NOT NULL,
+    status TEXT DEFAULT 'pending',
+    created_at REAL NOT NULL,
+    FOREIGN KEY (task_id) REFERENCES tasks(task_id)
+);
+
+CREATE INDEX IF NOT EXISTS idx_tasks_state ON tasks(state);
+CREATE INDEX IF NOT EXISTS idx_tasks_file ON tasks(task_file);
+CREATE INDEX IF NOT EXISTS idx_todos_task ON todos(task_id);
+"""
+
+
+class StateMachine:
+    """SQLite-backed state machine for the Cognitive Loop Engine."""
+
+    def __init__(self, db_path: str = "loop-engine/state/loop.db"):
+        self.db_path = Path(db_path)
+        self.db_path.parent.mkdir(parents=True, exist_ok=True)
+        self.conn = sqlite3.connect(str(self.db_path))
+        self.conn.row_factory = sqlite3.Row
+        self.conn.executescript(_SCHEMA)
+        self.conn.commit()
+
+    def close(self):
+        self.conn.close()
+
+    # --- Task State Operations ---
+
+    def register_task(self, task_file: str, state: TaskState = TaskState.BACKLOG) -> int:
+        """Register a new task file in the state machine."""
+        now = time.time()
+        cursor = self.conn.execute(
+            "INSERT OR IGNORE INTO tasks (task_file, state, created_at, updated_at) VALUES (?, ?, ?, ?)",
+            (task_file, state.value, now, now)
+        )
+        self.conn.commit()
+        return cursor.lastrowid or self.get_task_by_file(task_file)["task_id"]
+
+    def get_task(self, task_id: int) -> Optional[dict]:
+        row = self.conn.execute("SELECT * FROM tasks WHERE task_id = ?", (task_id,)).fetchone()
+        return dict(row) if row else None
+
+    def get_task_by_file(self, task_file: str) -> Optional[dict]:
+        row = self.conn.execute("SELECT * FROM tasks WHERE task_file = ?", (task_file,)).fetchone()
+        return dict(row) if row else None
+
+    def update_state(self, task_id: int, new_state: TaskState):
+        """Transition a task to a new state."""
+        now = time.time()
+        updates = {"state": new_state.value, "updated_at": now}
+        if new_state == TaskState.CLOSED:
+            updates["closed_at"] = now
+
+        set_clause = ", ".join(f"{k} = ?" for k in updates)
+        values = list(updates.values()) + [task_id]
+        self.conn.execute(f"UPDATE tasks SET {set_clause} WHERE task_id = ?", values)
+        self.conn.commit()
+
+    def set_plan(self, task_id: int, plan: str):
+        self.conn.execute("UPDATE tasks SET plan = ?, updated_at = ? WHERE task_id = ?",
+                          (plan, time.time(), task_id))
+        self.conn.commit()
+
+    def set_qa_feedback(self, task_id: int, feedback: str):
+        self.conn.execute(
+            "UPDATE tasks SET qa_feedback = ?, qa_retry_count = qa_retry_count + 1, updated_at = ? WHERE task_id = ?",
+            (feedback, time.time(), task_id))
+        self.conn.commit()
+
+    def increment_qa_retry(self, task_id: int) -> int:
+        """Increment QA retry count and return new count."""
+        cursor = self.conn.execute(
+            "UPDATE tasks SET qa_retry_count = qa_retry_count + 1, updated_at = ? WHERE task_id = ? RETURNING qa_retry_count",
+            (time.time(), task_id))
+        row = cursor.fetchone()
+        self.conn.commit()
+        return row[0] if row else 0
+
+    def get_qa_retry_count(self, task_id: int) -> int:
+        row = self.conn.execute("SELECT qa_retry_count FROM tasks WHERE task_id = ?", (task_id,)).fetchone()
+        return row[0] if row else 0
+
+    def set_evidence_dir(self, task_id: int, evidence_dir: str):
+        self.conn.execute("UPDATE tasks SET evidence_dir = ?, updated_at = ? WHERE task_id = ?",
+                          (evidence_dir, time.time(), task_id))
+        self.conn.commit()
+
+    def get_active_tasks(self) -> list[dict]:
+        """Get all tasks not in terminal states."""
+        rows = self.conn.execute(
+            "SELECT * FROM tasks WHERE state NOT IN ('closed', 'backlog') ORDER BY updated_at DESC"
+        ).fetchall()
+        return [dict(r) for r in rows]
+
+    def get_tasks_in_state(self, state: TaskState) -> list[dict]:
+        rows = self.conn.execute(
+            "SELECT * FROM tasks WHERE state = ? ORDER BY updated_at DESC", (state.value,)
+        ).fetchall()
+        return [dict(r) for r in rows]
+
+    # --- Todo Operations (Todo Enforcer) ---
+
+    def add_todo(self, task_id: int, description: str) -> int:
+        cursor = self.conn.execute(
+            "INSERT INTO todos (task_id, description, created_at) VALUES (?, ?, ?)",
+            (task_id, description, time.time()))
+        self.conn.commit()
+        return cursor.lastrowid
+
+    def update_todo_status(self, todo_id: int, status: str):
+        self.conn.execute("UPDATE todos SET status = ? WHERE id = ?", (status, todo_id))
+        self.conn.commit()
+
+    def get_pending_todos(self, task_id: int) -> list[dict]:
+        rows = self.conn.execute(
+            "SELECT * FROM todos WHERE task_id = ? AND status = 'pending' ORDER BY created_at",
+            (task_id,)).fetchall()
+        return [dict(r) for r in rows]
diff --git a/loop-engine/test_executor.py b/loop-engine/test_executor.py
new file mode 100644
index 0000000..6c9a0f0
--- /dev/null
+++ b/loop-engine/test_executor.py
@@ -0,0 +1,76 @@
+"""Tests for executor.py — Goal Plugin delegation with transport retry."""
+import sys, os
+sys.path.insert(0, os.path.dirname(__file__))
+
+from executor import TERM_COMPLETE, TERM_BLOCKED, TRANSPORT_ERROR, MAX_RETRIES, RETRY_DELAY
+from models import LoopEngineConfig
+
+
+def _cfg():
+    return LoopEngineConfig(approval={"chat_id": 123})
+
+
+def test_terminal_complete():
+    assert TERM_COMPLETE.search("Done! [goal:complete]") is not None
+    assert TERM_COMPLETE.search("No marker") is None
+
+
+def test_terminal_blocked():
+    assert TERM_BLOCKED.search("Cannot proceed [goal:blocked]") is not None
+    assert TERM_BLOCKED.search("No marker") is None
+
+
+def test_terminal_complete_multiline():
+    text = "Line 1\nLine 2\nTask done [goal:complete]\nLine 4"
+    assert TERM_COMPLETE.search(text) is not None
+
+
+def test_terminal_blocked_multiline():
+    text = "Error occurred\nCannot continue [goal:blocked]"
+    assert TERM_BLOCKED.search(text) is not None
+
+
+def test_transport_error_detection():
+    assert TRANSPORT_ERROR.search("stream disconnected before completion") is not None
+    assert TRANSPORT_ERROR.search("ECONNRESET") is not None
+    assert TRANSPORT_ERROR.search("ETIMEDOUT") is not None
+    assert TRANSPORT_ERROR.search("Connection reset by peer") is not None
+    assert TRANSPORT_ERROR.search("No transport error here") is None
+
+
+def test_retry_constants():
+    assert MAX_RETRIES == 3
+    assert RETRY_DELAY == 5
+
+
+def test_config_has_timeout():
+    cfg = _cfg()
+    assert cfg.idle.max_retries > 0
+    assert cfg.idle.thinking_timeout_seconds > 0
+
+
+def test_executor_instantiation():
+    from executor import HandsExecutor
+    from state import StateMachine
+    import tempfile
+    with tempfile.TemporaryDirectory() as tmp:
+        sm = StateMachine(os.path.join(tmp, "test.db"))
+        exe = HandsExecutor(_cfg(), sm)
+        assert exe.config is not None
+        assert exe.state is not None
+        sm.close()
+
+
+if __name__ == "__main__":
+    tests = [v for k, v in sorted(globals().items()) if k.startswith("test_")]
+    passed = failed = 0
+    for t in tests:
+        try:
+            t()
+            print(f"  PASS: {t.__name__}")
+            passed += 1
+        except Exception as e:
+            print(f"  FAIL: {t.__name__}: {e}")
+            failed += 1
+    print(f"\n{passed} passed, {failed} failed")
+    sys.exit(1 if failed else 0)
diff --git a/loop-engine/test_models.py b/loop-engine/test_models.py
new file mode 100644
index 0000000..6b84811
--- /dev/null
+++ b/loop-engine/test_models.py
@@ -0,0 +1,84 @@
+"""Tests for models.py — Pydantic config validation."""
+import sys, os
+sys.path.insert(0, os.path.dirname(__file__))
+
+from models import (
+    LoopEngineConfig, TaskState, CategoryConfig,
+    ProviderConcurrency, IdleConfig, ApprovalConfig
+)
+
+
+def test_task_state_values():
+    assert TaskState.BACKLOG.value == "backlog"
+    assert TaskState.IMPLEMENTING.value == "implementing"
+    assert TaskState.CLOSED.value == "closed"
+    assert TaskState.CRASHED.value == "crashed"
+    assert len(TaskState) == 10
+
+
+def test_category_config():
+    c = CategoryConfig(models=["kimi/kimi-k3"], description="test")
+    assert c.models == ["kimi/kimi-k3"]
+    assert c.reasoning is None
+
+
+def test_category_config_requires_models():
+    try:
+        CategoryConfig(models=[])
+        assert False, "Should have failed"
+    except Exception:
+        pass
+
+
+def test_provider_concurrency_defaults():
+    pc = ProviderConcurrency()
+    assert pc.anthropic == 3
+    assert pc.openai == 3
+
+
+def test_idle_config_defaults():
+    ic = IdleConfig()
+    assert ic.thinking_timeout_seconds == 60
+    assert ic.executing_timeout_seconds == 900
+    assert ic.max_retries == 5
+
+
+def test_approval_config():
+    ac = ApprovalConfig(chat_id=12345)
+    assert ac.bot_token_env == "TELEGRAM_BOT_TOKEN"
+    assert ac.chat_id == 12345
+
+
+def test_loop_engine_config_defaults():
+    cfg = LoopEngineConfig(approval={"chat_id": 123})
+    assert cfg.max_parallel_tasks == 1
+    assert "quick" in cfg.categories
+    assert "deep" in cfg.categories
+    assert cfg.max_qa_retries == 3
+
+
+def test_loop_engine_config_max_parallel_bounds():
+    try:
+        LoopEngineConfig(approval={"chat_id": 1}, max_parallel_tasks=5)
+        assert False, "Should fail: max is 4"
+    except Exception:
+        pass
+
+    cfg = LoopEngineConfig(approval={"chat_id": 1}, max_parallel_tasks=4)
+    assert cfg.max_parallel_tasks == 4
+
+
+if __name__ == "__main__":
+    tests = [v for k, v in sorted(globals().items()) if k.startswith("test_")]
+    passed = 0
+    failed = 0
+    for t in tests:
+        try:
+            t()
+            print(f"  PASS: {t.__name__}")
+            passed += 1
+        except Exception as e:
+            print(f"  FAIL: {t.__name__}: {e}")
+            failed += 1
+    print(f"\n{passed} passed, {failed} failed")
+    sys.exit(1 if failed else 0)
diff --git a/loop-engine/test_router.py b/loop-engine/test_router.py
new file mode 100644
index 0000000..c8e0856
--- /dev/null
+++ b/loop-engine/test_router.py
@@ -0,0 +1,93 @@
+"""Tests for router.py — LLM routing and context building."""
+import sys, os
+sys.path.insert(0, os.path.dirname(__file__))
+
+from router import LLMRouter, _load_file_if_exists
+from models import LoopEngineConfig
+
+
+def _make_config():
+    return LoopEngineConfig(approval={"chat_id": 123})
+
+
+def test_load_file_exists():
+    p = os.path.join(os.path.dirname(__file__), "models.py")
+    content = _load_file_if_exists(p)
+    assert "LoopEngineConfig" in content
+
+
+def test_load_file_missing():
+    content = _load_file_if_exists("/nonexistent/file.md")
+    assert content == ""
+
+
+def test_resolve_model_with_env():
+    os.environ["KIMI_API_KEY"] = "test-key"
+    cfg = _make_config()
+    router = LLMRouter(cfg)
+    model, reasoning = router._resolve_model("quick")
+    assert model == "kimi/kimi-k3"
+    del os.environ["KIMI_API_KEY"]
+
+
+def test_resolve_model_fallback():
+    for key in ["KIMI_API_KEY", "OPENAI_API_KEY", "ANTHROPIC_API_KEY", "GEMINI_API_KEY"]:
+        os.environ.pop(key, None)
+    cfg = _make_config()
+    router = LLMRouter(cfg)
+    model, reasoning = router._resolve_model("quick")
+    assert model == cfg.default_provider
+
+
+def test_build_system_context():
+    cfg = _make_config()
+    router = LLMRouter(cfg)
+    ctx = router._build_system_context("architect")
+    assert "Architect" in ctx
+    assert len(ctx) > 100
+
+
+def test_build_system_context_qa():
+    cfg = _make_config()
+    router = LLMRouter(cfg)
+    ctx = router._build_system_context("qa_engineer")
+    assert "QA Engineer" in ctx
+
+
+def test_route_plan():
+    cfg = _make_config()
+    router = LLMRouter(cfg)
+    routing = router.route_plan("## Goal\nBuild a feature", "quick")
+    assert routing["model"] is not None
+    assert routing["temperature"] == 0.3
+    assert "Build a feature" in routing["user"]
+
+
+def test_route_qa():
+    cfg = _make_config()
+    router = LLMRouter(cfg)
+    routing = router.route_qa("Task content", "diff here")
+    assert routing["temperature"] == 0.1
+    assert "diff here" in routing["user"]
+
+
+def test_route_review():
+    cfg = _make_config()
+    router = LLMRouter(cfg)
+    routing = router.route_review("Task", "QA passed")
+    assert routing["temperature"] == 0.2
+
+
+if __name__ == "__main__":
+    tests = [v for k, v in sorted(globals().items()) if k.startswith("test_")]
+    passed = failed = 0
+    for t in tests:
+        try:
+            t()
+            print(f"  PASS: {t.__name__}")
+            passed += 1
+        except Exception as e:
+            print(f"  FAIL: {t.__name__}: {e}")
+            failed += 1
+    print(f"\n{passed} passed, {failed} failed")
+    sys.exit(1 if failed else 0)
diff --git a/loop-engine/test_state.py b/loop-engine/test_state.py
new file mode 100644
index 0000000..fc70488
--- /dev/null
+++ b/loop-engine/test_state.py
@@ -0,0 +1,136 @@
+"""Tests for state.py — SQLite state machine."""
+import sys, os, tempfile
+sys.path.insert(0, os.path.dirname(__file__))
+
+from state import StateMachine
+from models import TaskState
+
+
+def test_register_task():
+    with tempfile.TemporaryDirectory() as tmp:
+        sm = StateMachine(os.path.join(tmp, "test.db"))
+        tid = sm.register_task("tasks/backlog/01-test.md", TaskState.BACKLOG)
+        assert tid is not None
+        task = sm.get_task(tid)
+        assert task["state"] == "backlog"
+        assert task["task_file"] == "tasks/backlog/01-test.md"
+        sm.close()
+
+
+def test_get_task_by_file():
+    with tempfile.TemporaryDirectory() as tmp:
+        sm = StateMachine(os.path.join(tmp, "test.db"))
+        sm.register_task("tasks/backlog/02-feature.md")
+        task = sm.get_task_by_file("tasks/backlog/02-feature.md")
+        assert task is not None
+        assert task["task_id"] > 0
+        sm.close()
+
+
+def test_update_state():
+    with tempfile.TemporaryDirectory() as tmp:
+        sm = StateMachine(os.path.join(tmp, "test.db"))
+        tid = sm.register_task("tasks/backlog/03-test.md")
+        sm.update_state(tid, TaskState.PLANNING)
+        task = sm.get_task(tid)
+        assert task["state"] == "planning"
+        sm.update_state(tid, TaskState.IMPLEMENTING)
+        task = sm.get_task(tid)
+        assert task["state"] == "implementing"
+        sm.close()
+
+
+def test_set_plan():
+    with tempfile.TemporaryDirectory() as tmp:
+        sm = StateMachine(os.path.join(tmp, "test.db"))
+        tid = sm.register_task("tasks/backlog/04-test.md")
+        sm.set_plan(tid, "## Plan\n1. Do stuff")
+        task = sm.get_task(tid)
+        assert task["plan"] == "## Plan\n1. Do stuff"
+        sm.close()
+
+
+def test_qa_feedback_and_retry():
+    with tempfile.TemporaryDirectory() as tmp:
+        sm = StateMachine(os.path.join(tmp, "test.db"))
+        tid = sm.register_task("tasks/backlog/05-test.md")
+        sm.set_qa_feedback(tid, "Fix the bug on line 42")
+        task = sm.get_task(tid)
+        assert task["qa_retry_count"] == 1
+        assert task["qa_feedback"] == "Fix the bug on line 42"
+        count = sm.increment_qa_retry(tid)
+        assert count == 2
+        sm.close()
+
+
+def test_closed_at_timestamp():
+    with tempfile.TemporaryDirectory() as tmp:
+        sm = StateMachine(os.path.join(tmp, "test.db"))
+        tid = sm.register_task("tasks/backlog/06-test.md")
+        sm.update_state(tid, TaskState.CLOSED)
+        task = sm.get_task(tid)
+        assert task["closed_at"] is not None
+        assert task["closed_at"] > 0
+        sm.close()
+
+
+def test_get_active_tasks():
+    with tempfile.TemporaryDirectory() as tmp:
+        sm = StateMachine(os.path.join(tmp, "test.db"))
+        sm.register_task("tasks/backlog/07a.md")
+        tid2 = sm.register_task("tasks/backlog/07b.md")
+        sm.update_state(tid2, TaskState.IMPLEMENTING)
+        active = sm.get_active_tasks()
+        assert len(active) == 1
+        assert active[0]["task_file"] == "tasks/backlog/07b.md"
+        sm.close()
+
+
+def test_get_tasks_in_state():
+    with tempfile.TemporaryDirectory() as tmp:
+        sm = StateMachine(os.path.join(tmp, "test.db"))
+        t1 = sm.register_task("tasks/backlog/08a.md")
+        t2 = sm.register_task("tasks/backlog/08b.md")
+        sm.update_state(t1, TaskState.QA)
+        sm.update_state(t2, TaskState.QA)
+        qa_tasks = sm.get_tasks_in_state(TaskState.QA)
+        assert len(qa_tasks) == 2
+        sm.close()
+
+
+def test_todos():
+    with tempfile.TemporaryDirectory() as tmp:
+        sm = StateMachine(os.path.join(tmp, "test.db"))
+        tid = sm.register_task("tasks/backlog/09-test.md")
+        todo_id = sm.add_todo(tid, "Write tests")
+        assert todo_id > 0
+        pending = sm.get_pending_todos(tid)
+        assert len(pending) == 1
+        sm.update_todo_status(todo_id, "done")
+        pending = sm.get_pending_todos(tid)
+        assert len(pending) == 0
+        sm.close()
+
+
+def test_register_task_idempotent():
+    with tempfile.TemporaryDirectory() as tmp:
+        sm = StateMachine(os.path.join(tmp, "test.db"))
+        t1 = sm.register_task("tasks/backlog/10-test.md")
+        t2 = sm.register_task("tasks/backlog/10-test.md")
+        assert t1 == t2  # same file = same task
+        sm.close()
+
+
+if __name__ == "__main__":
+    tests = [v for k, v in sorted(globals().items()) if k.startswith("test_")]
+    passed = failed = 0
+    for t in tests:
+        try:
+            t()
+            print(f"  PASS: {t.__name__}")
+            passed += 1
+        except Exception as e:
+            print(f"  FAIL: {t.__name__}: {e}")
+            failed += 1
+    print(f"\n{passed} passed, {failed} failed")
+    sys.exit(1 if failed else 0)
diff --git a/loop-engine/watcher.py b/loop-engine/watcher.py
new file mode 100644
index 0000000..f1ab706
--- /dev/null
+++ b/loop-engine/watcher.py
@@ -0,0 +1,128 @@
+"""
+Kanban Watcher — detects new task files in tasks/backlog/.
+
+Uses Python watchdog filesystem observer.
+Read-only: never modifies task files.
+Triggers the pipeline by registering the task in StateMachine.
+
+Inspired by OMO's rules-engine but minimal:
+- Only watches tasks/backlog/
+- Ignores tasks/archive/, loop-engine/, .git/
+"""
+
+import re
+import time
+from pathlib import Path
+from typing import Callable, Optional
+
+from watchdog.observers import Observer
+from watchdog.events import FileSystemEventHandler, FileCreatedEvent
+
+from models import TaskState
+from state import StateMachine
+
+
+def _parse_task_metadata(file_path: str) -> Optional[dict]:
+    """Extract task ID, title, source, type, status from a task file."""
+    path = Path(file_path)
+    if not path.suffix == ".md":
+        return None
+
+    content = path.read_text(encoding="utf-8")
+
+    # Extract task ID from filename: 01-feature-name.md → 01
+    match = re.match(r'^(\d+)-', path.stem)
+    if not match:
+        return None
+    task_id = int(match.group(1))
+
+    # Extract metadata headers
+    metadata = {"task_id": task_id, "file": str(path)}
+
+    for field in ["Source", "Type", "Status"]:
+        m = re.search(rf'\*\*{field}:\*\*\s*(.+)', content)
+        if m:
+            metadata[field.lower()] = m.group(1).strip()
+
+    return metadata
+
+
+class BacklogHandler(FileSystemEventHandler):
+    """Watches for new .md files in tasks/backlog/."""
+
+    def __init__(self, state: StateMachine, on_task_detected: Optional[Callable] = None):
+        self.state = state
+        self.on_task_detected = on_task_detected
+
+    def on_created(self, event):
+        if event.is_directory:
+            return
+
+        file_path = event.src_path
+
+        # Only watch .md files in tasks/backlog/
+        if not file_path.endswith(".md"):
+            return
+        if "tasks/backlog/" not in file_path:
+            return
+
+        # Ignore archive, loop-engine, .git
+        for ignore in ["archive", "loop-engine", ".git"]:
+            if ignore in file_path:
+                return
+
+        # Parse metadata
+        meta = _parse_task_metadata(file_path)
+        if not meta:
+            return
+
+        # Register in state machine
+        task_record = self.state.get_task_by_file(file_path)
+        if not task_record:
+            task_id = self.state.register_task(file_path, TaskState.BACKLOG)
+            print(f"[watcher] New task detected: {file_path} (ID: {task_id})")
+
+            if self.on_task_detected:
+                self.on_task_detected(task_id, file_path)
+
+
+class KanbanWatcher:
+    """Filesystem observer for tasks/backlog/."""
+
+    def __init__(self, state: StateMachine, tasks_dir: str = "tasks",
+                 on_task_detected: Optional[Callable] = None):
+        self.state = state
+        self.tasks_dir = Path(tasks_dir)
+        self.backlog_dir = self.tasks_dir / "backlog"
+        self.observer = Observer()
+        self.handler = BacklogHandler(state, on_task_detected)
+
+    def start(self):
+        """Start watching tasks/backlog/ for new files."""
+        self.backlog_dir.mkdir(parents=True, exist_ok=True)
+        self.observer.schedule(self.handler, str(self.backlog_dir), recursive=False)
+        self.observer.start()
+        print(f"[watcher] Watching {self.backlog_dir} for new tasks...")
+
+    def stop(self):
+        self.observer.stop()
+        self.observer.join()
+
+    def scan_existing(self) -> list[dict]:
+        """Scan tasks/backlog/ for existing unregistered tasks."""
+        detected = []
+        if not self.backlog_dir.exists():
+            return detected
+
+        for md_file in sorted(self.backlog_dir.glob("*.md")):
+            meta = _parse_task_metadata(str(md_file))
+            if not meta:
+                continue
+
+            task_record = self.state.get_task_by_file(str(md_file))
+            if not task_record:
+                task_id = self.state.register_task(str(md_file), TaskState.BACKLOG)
+                detected.append({"task_id": task_id, "file": str(md_file)})
+                print(f"[watcher] Existing task registered: {md_file.name} (ID: {task_id})")
+
+        return detected
diff --git a/opencode.json b/opencode.json
index 62f7e9d..5536ac7 100644
--- a/opencode.json
+++ b/opencode.json
@@ -2,6 +2,7 @@
   "$schema": "https://opencode.ai/config.json",
   "default_agent": "cognitive-executor",
   "instructions": ["docs/opencode-shell-strategy.md"],
+  "plugin": ["@prevalentware/opencode-goal-plugin"],
   "mcp": {
     "custom_context": {
       "type": "local",
```
<!-- END_GIT_DIFF -->
- [x] Step 11 (2026-08-21): Investigate Goal Plugin — read full source (3196 lines), compare reliability (wins 7/9)
- [x] Step 12 (2026-08-21): Refactor executor.py — delegate auto-continue to Goal Plugin
- [x] Step 13 (2026-08-21): Add pyproject.toml with dependencies
- [x] Step 14 (2026-08-21): Create .env.example with required API keys
- [x] Step 15 (2026-08-21): Add Goal Plugin to opencode.json
- [x] Step 16 (2026-08-21): Install dependencies, verify all imports
- [x] Step 17 (2026-08-21): Run all tests — 35/35 pass
- [x] Step 18 (2026-08-21): Smoke test daemon.py config loading
