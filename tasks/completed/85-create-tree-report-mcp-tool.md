# Task 85: Add Tree Report MCP Tool

**File:** `tasks/completed/85-create-tree-report-mcp-tool.md`
**Source:** manager
**Type:** feature
**Status:** closed

## Source Context

### Variant C: Manager (`**Source:** manager`)

## Goal

Add a `create_tree_report` tool to `mcp-context-server/server.py` that generates a `.gitignore`-aware directory tree of a target path (default: the entire project) and saves it as a Markdown file under `context-reports/` named `tree_report_<timestamp>.md`, mirroring the existing `context_report_<timestamp>.md` convention. Sync every project artifact that documents the MCP tool surface (README, AGENTS.md, system-prompt.md templates, agents, code-search skill, tests, CHANGELOG).

## Manager's Notes

- Trigger phrase the AI must recognize: "create a tree of the project" (or "... of <path>").
- Tree reports follow the same lifecycle as context reports: generated by the MCP server, never read inline by the agent, file path handed to the Manager/Orchestrator.
- Implementation performed ad-hoc by the Executor under the `cognitive-executor` agent; this file serves as the record for the Orchestrator's later QA / Code-Review / closure cycle.

## Local TODOs

- [x] Add `create_tree_report` tool to `mcp-context-server/server.py` (reuses `GitIgnoreFilter` + `generate_tree`; shares the `_ensure_context_reports_ignored()` safeguard with `read_source_files`; numeric suffix prevents same-second overwrites)
- [x] Add regression tests to `tests/test_mcp_servers.py` (explicit path, default target, invalid path, `.gitignore` filtering, same-second collision)
- [x] Update `README.md` (How It Works + Available Tools sections)
- [x] Update `AGENTS.md` context-reports guardrail
- [x] Update `system-prompt.md` discovery/combined task templates + bump `<system_version>` 8.3.0 → 8.4.0
- [x] Update `agents/cognitive-executor.md` and `agents/cognitive-discovery.md`
- [x] Update `skill-templates/code-search/SKILL.md`
- [x] Add `## [Unreleased]` → `### Added` entry to `CHANGELOG.md`
- [x] Update `skill-templates/audit-agents/SKILL.md` (MCP Report Generation audit criterion ×2 + AGENTS.md template guardrail)
- [x] Sync global skill copies (`~/.config/opencode/skills/code-search` + `audit-agents`) with the repo templates

## Acceptance Criteria

- [x] `create_tree_report` tool exists on the `custom_context` server and saves `tree_report_*.md` under `context-reports/`
- [x] Default invocation with no args trees the entire project; explicit `target_path` trees any sub-path
- [x] Tree respects `.gitignore` (ignored entries absent)
- [x] Rapid consecutive calls never overwrite each other (UUID suffix)
- [x] All related docs/skills/agents/templates updated; system prompt version bumped to 8.4.0
- [x] `audit-agents` skill carries the MCP Report Generation criterion; global skill copies (`code-search`, `audit-agents`) synced from templates
- [x] New pytest regression tests pass alongside the existing suite

## Verification Evidence

- **Test command:** `uv run --with pytest --with pathspec --with "mcp[cli]>=1.0,<2.0" --with pyyaml --with tree-sitter --with tree-sitter-python --with tree-sitter-javascript --with tree-sitter-typescript --with tree-sitter-go --with tree-sitter-java --with tree-sitter-rust --with tree-sitter-kotlin pytest tests/test_mcp_servers.py -q` + `python3 -m py_compile mcp-context-server/server.py tests/test_mcp_servers.py`
- **Expected result:** 14 tests passed (12 prior + 2 new security tests: path traversal rejection, None input handling); syntax compile OK
- **Actual result:** `14 passed, 9 warnings in 1.11s`; `✅ Syntax OK`; live demo against repo root returned `✅ Success: Directory tree saved for '.'. 📁 Generated Report: context-reports/tree_report_20260809_135959.md`
- **Exit code:** 0

## Risk & Rollback

- **Risk:** Tool writes into `context-reports/` — mitigated by the shared auto-gitignore safeguard; reports are never committed.
- **Rollback plan:** Remove the `create_tree_report` tool function and revert the doc updates via `git revert` of the feature commit (Manager-approved closure path).

---

## OpenCode Execution Log & Reasoning

### What was done

1. **`mcp-context-server/server.py`** — Added `create_tree_report(target_path: str = ".")`:
   - Reuses the existing `.gitignore`-aware `GitIgnoreFilter` and `generate_tree()` engine — zero new tree logic.
   - Default `.` trees the entire project; any sub-path can be targeted.
   - Saves `context-reports/tree_report_<timestamp>_<uuid>.md` (UUID suffix guarantees collision-free naming; the earlier numeric-suffix `exists()` loop was replaced during the QA iteration — see below).
   - Extracted the `.gitignore` safeguard from `read_source_files` into a shared `_ensure_context_reports_ignored()` helper so both report-producing tools protect `context-reports/` from being committed.
   - Docstring tuned to the natural trigger: "create a tree of the project" / "create a tree of <path>".
2. **`tests/test_mcp_servers.py`** — Added 6 regression tests: explicit-path + `.gitignore` filtering + safeguard, default whole-project target, invalid (non-directory) target, same-second collision (two rapid calls → two distinct files), path traversal rejection, and None input handling. Restored the pre-existing `test_stage_and_inject_diff_with_ignored_context_reports` after an editing slip, preserving the full suite (14 tests).
3. **Docs sync** — `README.md` (How It Works + Available Tools), `AGENTS.md` guardrail, `system-prompt.md` (both discovery templates gained step 1.5 "PERSIST THE TREE"; version bumped 8.3.0 → 8.4.0 MINOR), `agents/cognitive-executor.md`, `agents/cognitive-discovery.md`, `skill-templates/code-search/SKILL.md`, `CHANGELOG.md` (`[Unreleased]` → `### Added`).

### Architectural reasoning

- The tool is a **persistence twin** of `get_directory_tree`: the inline tool stays for interactive exploration; `create_tree_report` gives the Manager/Orchestrator a durable artifact that can be opened or pasted back to the Brain, exactly like `read_source_files` reports.
- Reusing `generate_tree` + `GitIgnoreFilter` keeps behavior identical to the inline tree — no divergence risk between "what you see" and "what gets saved".
- The shared safeguard helper keeps the "reports are never committed" invariant in ONE place instead of two copies.

### Skill ecosystem audit & sync (Manager follow-up)

- Audited all 29 skill templates for references to the MCP tool surface: only `code-search` and `audit-agents` are affected.
- `skill-templates/audit-agents/SKILL.md` updated in 3 spots: the top Target Audit Criteria summary, the Mode 1 `AGENTS.md` generation template (new guardrail pair matching the repo's own AGENTS.md), and the Mode 2 audit criteria — all now carry the **MCP Report Generation** requirement (context reports via `read_source_files`, tree reports via `create_tree_report`, never read inline).
- Deployed global copies synced: `~/.config/opencode/skills/code-search/SKILL.md` (gained step 1.5 from the previous session) and `~/.config/opencode/skills/audit-agents/SKILL.md` — future executor sessions now load the updated skills.

### QA Fix Iteration (Path Traversal / TOCTOU / None Input) — QA Engineer findings

- [x] **Step 1:** Fix Path Traversal in `create_tree_report` — resolve `target_path` against `Path.cwd().resolve()`; reject paths escaping the workspace with `"Error: Path traversal detected. target_path must be within the project workspace."`; coerce `None`/invalid types to `"."`.
- [x] **Step 2:** Fix TOCTOU race condition — replaced the `while report_file.exists():` check loop with a UUID suffix (`uuid4().hex[:8]`) appended to the timestamp for guaranteed collision-free naming.
- [x] **Step 3:** Add regression tests `test_create_tree_report_rejects_path_traversal` and `test_create_tree_report_handles_none_input`.
- [x] **Step 4:** Log the security fixes in the OpenCode Execution Log (this section) + CHANGELOG `### Fixed` entry.

### Security fixes applied (QA Engineer adversarial findings)

1. **Path Traversal prevention** — `create_tree_report` now resolves `target_path` via `Path(target_path).resolve()` and verifies the result is inside the workspace root (`Path.cwd().resolve()`) using `relative_to`. Any path escaping the project returns `"Error: Path traversal detected. target_path must be within the project workspace."` before any directory walk or file write. The `read_source_files` tool already had its own `context-reports`-scope guard, but `create_tree_report` accepted arbitrary absolute paths — now closed.
2. **TOCTOU race condition** — the previous unique-name strategy (`while report_file.exists(): counter += 1`) is a check-then-act pattern: two concurrent calls could both see the same free name and overwrite each other. Replaced with a UUID suffix (`uuid.uuid4().hex[:8]` appended to the timestamp) so filenames are unique by construction — no existence check needed. The same-second collision regression test still passes (now via UUID uniqueness).
3. **None / invalid type handling** — a malformed invocation (`create_tree_report(None)`) previously raised `TypeError` inside `Path(None)`. Now `if not isinstance(target_path, str): target_path = "."` degrades gracefully to the whole-project default, covered by `test_create_tree_report_handles_none_input`.
4. **Doc sync for the naming change** — every artifact documenting the report filename (`README.md`, `system-prompt.md` ×2 templates, `agents/cognitive-discovery.md`, `skill-templates/code-search/SKILL.md` + global copy, `CHANGELOG.md`) updated from `tree_report_<timestamp>.md` to `tree_report_<timestamp>_<uuid>.md`; CHANGELOG gained a `### Fixed` entry (Parse-Then-Append protocol).

## Factual Git Diff

<!-- BEGIN_GIT_DIFF -->
**Factual Git Diff:** Stored in Commit Hash: `ea54ba6e66e9012a5f51e6f4d9ee1e8e2f09dff1`
<!-- END_GIT_DIFF -->
