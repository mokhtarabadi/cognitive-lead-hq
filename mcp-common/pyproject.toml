[project]
name = "mcp-common"
version = "1.0.0"
description = "Shared helpers for Cognitive Lead AI MCP servers (env loading)"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mcp_common"]
